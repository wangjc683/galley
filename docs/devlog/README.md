# Devlog

Galley 开发日志：记录设计与工程决策的"为什么"，以及考虑过但被否的方案。

补充于 PRD（产品定义）、DESIGN.md（设计规则）、CLAUDE.md（项目宪法）—— devlog 提供历史叙事和 decision provenance。git log 太短只说"是什么"，PRD 太静态只说"现在是什么"，devlog 才记录"我们怎么走到这里的"。

## 时间线

| 日期 | 主题 | 摘要 |
|---|---|---|
| 2026-05-07 | [Stage 1 Bridge POC 完成](./2026-05-07-stage1-bridge-poc-complete.md) | IPC 协议 v0.1 落地 + WorkbenchHandler 双轨制 + 主入口 + 5 项 e2e 全过 |
| 2026-05-07 | [设计方向转向 Notion + Claude](./2026-05-07-design-direction-pivot.md) | 从 dark/Linear 风转向 light/文档对话工作台；9 块设计基础对齐 |
| 2026-05-08 | [首次体验三连 + LLM 切换](./2026-05-08-onboarding-and-llm-switching.md) | Onboarding wizard / Empty state hero composer / Health Check Card 设计；LLM 切换工程层完成（IPC + bridge + 测试） |
| 2026-05-08 | [设计三连收尾 + file_patch diff + Error hint](./2026-05-08-design-trio-finale.md) | Error Card / Command Palette / Settings 设计 + file_patch diff 视图加入 V0.1 + ErrorEvent 四字段扩展（IPC + bridge + 测试）+ DESIGN.md v0.2 完整版定稿 |
| 2026-05-08 | [Stage 2 桌面端骨架完成](./2026-05-08-stage2-desktop-skeleton-complete.md) | Tauri v2 + React 19 + Tailwind v4 + Zustand + SQLite + Python bridge IPC 端到端串通；11 个子任务（#1-#10b）一气呵成；@pierre/diffs reversal；conversation source-of-truth 优先级 |
| 2026-05-09 | [Project 模型 · coding agent 用户的归类容器](./2026-05-09-project-model-coding-agent.md) | Project = 纯归类抽屉（不绑 instructions / 不改变 GA 内核体验）；schema 加 pinned + lastActivityAt；DESIGN.md sidebar Project Section Spec A-G + Project View 二级页面；migration 策略首次明确（V0.1 release 前直改 001） |
| 2026-05-09 | [YOLO Mode · 审批是出口而非围栏](./2026-05-09-yolo-mode.md) | PRD §6.1 #4 重新表述（审批是出口）+ §11.5 加 YOLO Mode；命名 / TopBar persistent indicator / activation modal 文案 / bridge needs_approval 优先级；IPC `set_yolo_mode` 命令；prefs API 通用化；5 个新 bridge test 全过 |
| 2026-05-09 | [Stage 3 #1 端到端真跑 + 一波 dogfood UX polish](./2026-05-09-stage3-end-to-end-and-ux-polish.md) | 16 个 commits 把端到端真跑打通 + dogfood polish + 提前做 V0.2 范围（Markdown + Shiki / Message actions / 流式生成 + sticky-bottom）；spawn capability fix / drag region / 7 个跑通过程暴露的 bug；SoftHr 4 轮迭代后干脆删；V0.1 七件事剩 #2 + #5 |
| 2026-05-11 | [Stage 3 multi-session：N-active + useShallow 踩坑 + LRU 5](./2026-05-11-stage3-multi-session-and-perf.md) | tool_events 审批审计持久化（v0.1 scope）+ N-active 多进程并存架构（1-active 被用户一票否决）+ useShallow 反模式踩坑（React 19 strict mode getSnapshot 死循环→app 空白；改 store-side enrichment 修复）+ LRU 5 资源策略拍板（待 Task 3 配套）+ launcher 调研给 Task 3 留 `set_state` 协议参考；撤回沉淀 Skill chip（违反 GA non-invasive 哲学） |
| 2026-05-11 | [Stage 3 V0.1 收尾 + dogfood 7 轮 UX 打磨](./2026-05-11-stage3-v0.1-completion.md) | 14 个 commits 把 V0.1 七件事代码层做齐：Multi-session polish 4 项 / Session Restore（user message turn_index = turnCount+1 + ready 触发 replayHistoryToBridge）/ LRU 5 alive + active 保护 / Settings path picker（Python 字段诚实改只读）/ Onboarding fs.exists 5 项 health check / macOS bundle bridge/ 作 Tauri resource；然后第一次跑 dev 真实体验，7 轮 dogfood 反馈：composer auto-grow / LLM 内联 Popover / 右键 Archive + toast / lazy New Chat + 清「新对话」累积 / 软化 thinking placeholder + strip GA `LLM Running` marker / 「第 N 轮」→「第 N 步」/ Sidebar 三状态 unread / 修复 turn summary 静默丢失 |
| 2026-05-12 | [Stage 3 dogfood polish marathon + turn_index 双层语义拆分](./2026-05-12-dogfood-polish-marathon.md) | 17 个 commits 第二轮 dogfood 收尾。关键 critical fix: GA 每条 user_message 的 `agent_runner_loop` 都从 turn=1 重置 → SQLite ON CONFLICT 静默覆盖老 assistant row → conversation 错乱。修法拆 turn_index 双层语义（DB absolute / UI per-message GA 原生）+ rowsToTurns 反推 base，零 migration。其它 polish: LRU 加 agentRunning 保护 / streaming 三件套（typing dots + cursor + fake typewriter）/ Archive 系统完整化（双层 destructive confirm）/ Tool callout 哲学讨论后统一 inline pill / Sidebar 三态 subline（运行 `正在工作 · 第 N 步` + 完成 `已完成 · summary`）/ AgentTurn.summary v3 migration / LLM list 持久化到 prefs cold-start / LLM picker footer hint「修改 mykey.py 后重启 Workbench 生效」/ Copy/Save 中间冒出三层 null 防御 |
| 2026-05-13 | [Sidebar IA 重塑 · FTS5 全文搜索 · Inspector 退役 · Projects V0.1 · GA baseline cf65515](./2026-05-13-sidebar-overhaul-and-projects.md) | 一 session 跨 8 个主题：Sidebar Earlier 桶折叠 + EarlierDialog（月分组 + 多选 bulk archive）+ SQLite FTS5 全文搜索（migration 004，trigram tokenizer，CommandPalette 内 "在对话内容中" 分区）/ Inspector 整面退役（第一性原理：每 tab 都重复其它地方信息，回收 14-30% 横向空间）/ AppShell overflow-hidden 修复（Composer 被推上去 bug）/ MessageActions icon-only + Radix Tooltip（100ms 即时反馈）/ Multi-select bulk in EarlierDialog & ArchivedDialog（Gmail-style Select toggle）/ GA Baseline 6a3eecc → cf65515 升级（92 commits，逐项审计零 breaking change）/ **Projects V0.1 完整实现**（5 个 phase：数据层 + 创建 dialog + sidebar 渲染 + 分配/filter + CWD 绑定 + edit/delete + 审批入口恢复 + ProjectsDialog + 右键 Delete 红色 destructive + filter banner 显示 rootPath）；多个 IA 大决策：拒绝双侧边栏 / Settings 不装 session 级 cwd / Recent decisions 在 Settings 里抽象层错位整段删除 |
| 2026-05-13 | [Scroll-on-completion 方案 E 暂存](./2026-05-13-scroll-on-completion-deferred.md) | 长任务出最终答案后自动 scroll 到底部 → 用户要手动往上找答案开头。讨论 A-E 共 5 个方案：默认 read mode 被否（长任务期间分辨不出 GA 完成没）→ 落到方案 E（默认 read + `run_complete` smooth scroll 到 `[data-role="final-answer"]` wrapper）。暂不实施，等 beta/公测验证痛点频度。entry 含完整 implementation outline 5 步，revisit 时直接照做。 |
| 2026-05-13 | [Project 绑定文件夹 hint 文案 + cwd live-sync 暂存](./2026-05-13-project-cwd-copy-and-live-sync-deferred.md) | Create/Edit dialog 项目文件夹 hint 漏满架构术语（cwd / GA 子进程 / rootPath / bridge）→ 重写成基于「工作区」概念的中文白话。Create: 「项目里的对话以此文件夹为工作区」；Edit: 「修改后已有对话需重启 Workbench 后生效」。关键 reframing：hint 应解释**反直觉**的部分，不浪费字解释 default behavior。讨论过加「重启此对话」右键菜单 / 自动 respawn / toast 提示，全否（要么术语泄漏要么毁用户工作）。Future path: IPC `set_cwd` + bridge `os.chdir` 实现 live-sync 而非重启 app；约 200-300 行工程，等 beta/公测有真实痛点再启动。 |
| 2026-05-13 | [GA Baseline 升级 cf65515 → 6bb3104](./2026-05-13-ga-baseline-upgrade-cf65515-to-6bb3104.md) | 写完 [Baseline Upgrade Workflow](../../CLAUDE.md) 当天就实跑首次：upstream 5 个 commits（4 个 tui/docs + 1 个 `feat: dynamic tool_result maxlen`），最后那个 commit 3205f4a 把 `BaseHandler.dispatch` 签名加了 `tool_num=1` 参数 = **breaking change**。桥接层 `WorkbenchHandler.dispatch` 加 `tool_num` 透传 super 适配。80/80 tests pass，mypy strict + ruff clean。流程本身没漏没冗余，四个接口表面 checklist 在这次正好命中 dispatch 变化。也暴露一个 TODO：现有 dispatch 测试只用默认参数调，没测显式传 `tool_num` 的 case，应补一条 fail-fast 保护。 |
| 2026-05-13 | [Baseline regression · 改用 feature detection 适配](./2026-05-13-baseline-regression-and-feature-detection.md) | 上一条 baseline 升级 ship 当天就翻车：硬绑新 GA 签名的 `WorkbenchHandler.dispatch` 在用户的旧 GA（d2840ae，breaking change 之前）上炸 `TypeError`，整个 agent loop 死掉，desktop 卡「思考中」永远清不掉。改用 `inspect.signature` 做 feature detection（运行时探测 BaseHandler.dispatch 是否支持 tool_num），新旧 GA 双向兼容。**关键 methodology 沉淀进 CLAUDE.md Baseline Upgrade Workflow**：(a) 接口适配优先用 inspect-based feature detection 而不是硬绑签名；(b) 跑测试矩阵必须新旧 GA 两端都过，单跑一端不算数。 |
| 2026-05-13 | [LLM warmup bridge · 启动时自动刷新模型列表](./2026-05-13-llm-warmup-bridge.md) | Bug：改 mykey.py 加新模型后重启 Workbench，New Chat 看不到新模型；进任意已有 session 才会触发。根因：bridge lazy spawn，冷启动没任何 bridge 跑 → `state.llms` 永远是 prefs 旧缓存。讨论 A-D 共 4 个方案，落到 A（启动后台跑一次性 warmup bridge 拿 list_llms()，shutdown）。`hydrateFromDB` 末尾 + `setGAConfig` 内重置 flag 后双触发；用 `_warmupComplete` 字段 dedupe；race-condition 用 `pendingShutdown` flag 处理；15s timeout 兜底。复用现有 bridge spawn 基础设施不引入新 Python 入口。 |
| 2026-05-13 | [V0.2 增量 · /btw 侧问 · /branch 分支 · /rewind 悔棋](./2026-05-13-v0.2-side-question-branch-rewind.md) | GA upstream tuiapp_v2.py 启发的三个新交互原语，提案进 V0.2。`/btw`：用 worker thread + `backend.raw_ask` 跑独立 sub-agent 不动主 history，新增 IPC `side_question` / `side_question_response`，UI 黄色侧边条气泡 + 不计入 Turn 编号 + replayHistoryToBridge 跳过持久化。`/branch`：零新 IPC，复用 spawn + load_history（接受现有 lossy adapter），sidebar 右键 + 新 session 顶部"分支自 #X" system message。`/rewind <n>`：新增 IPC `rewind` / `rewound`，bridge 端 `history[:] = history[:cut]` truncate，desktop 非破坏性 marker（migration 005 加 `rewound` column）。Rejected：monkey-patch / desktop-only fake rewind / destructive SQL delete / in-process branch / ChoiceList for LLM（我们 LLMPill 更好）。Open：`/btw` 多轮？ChoiceList 是否给 `ask_user` 用（V0.1 漏渲染）？Edit Message 与 /rewind 共享语义？load_history adapter 富化是否前置？ |
| 2026-05-13 | [Galley 重命名 + 多项 V0.1 功能马拉松](./2026-05-13-galley-rename-and-features-marathon.md) | 超长 session 一次性把多件事推完：(1) ask_user UI 实施（AskUserBubble 黄色气泡 + 候选 chips + Sidebar 第四态 "⏸ 等你回复" + 抑制 ask_user tool callout）；(2) Composer 大段粘贴折叠（>10 行触发 `[Pasted text #N +M lines]` 占位符）；(3) 折叠完成 turn / Sidebar 折叠 / TopBar Search 全部 park 或删除（cargo cult 警觉）；(4) Conversation 宽度 toggle（紧凑 760 / 宽松 1200，全局 prefs，TopBar pill 跟 YOLO 同视觉族）；(5) Sidebar header 重塑：GALLEY semibold uppercase + 右对齐 `● GA 就绪` 真信号化（之前 `Runtime · healthy` 是装饰品，重写为 ready/unconfigured 二态，未配置可点跳 Settings → Runtime）；(6) **品牌重命名 GA Workbench → Galley**：14 个文件，origin story "Galley started as a workbench for GenericAgent. The first two letters of our name are a quiet bow to where we came from."；(7) Tauri identifier 改动副作用事故 + 教训写进 CLAUDE.md（公开 release 前要 Rust 自动迁移）；(8) **CLAUDE.md 项目宪法重写**："不读写" → "不修改 + 读取分级"——只读 GA 内部文件允许（标 coupling 点 + baseline 升级审计）；(9) Reinject Tools 实施（IPC + bridge handler + ⋯ 菜单）；(10) Desktop Pet 实施（subprocess spawn + sticky-B + turn_end hook + ⋯ 菜单）；(11) TopBar `⋯` overflow menu 从右 cluster 移到 title 旁边，session-scoped（无 active session 时隐藏）；(12) 手动 session rename（右键 ✏️ + inline edit input + Enter/Esc/blur）；(13) 空闲自主行动 + AI rename C 都设计了但 defer 到 V0.2。 |
| 2026-05-13 | [V0.2 增量 · AI Session Rename (user-triggered)](./2026-05-13-v0.2-ai-session-rename.md) | 手动 rename 之外的 LLM 增援。Option B（auto-trigger）否决 → Option C（user-triggered）保留。设计：⋯ 菜单加 ✨ AI 重命名，调当前 session active LLM 通过 `backend.raw_ask`（复用 /btw 同款 deepcopy + 不写 history 模式）出 8-12 字中文标题。Prompt 显式禁止 meta 措辞 + 强调名词短语。IPC: `rename_via_llm` / `session_renamed`。~110 LOC（比手动 rename 还少，复用基础设施）。Open：prompt 是否预先 dogfood？要不要 5 秒撤销 toast？上下文取多少轮？ |
| 2026-05-13 | [V0.2 增量 · 空闲自主行动 (Idle Autonomy)](./2026-05-13-v0.2-idle-autonomy.md) | GA 官方 "30 分钟无活动自动注入 [AUTO]🤖 prompt" 在 Galley 的适配设计。决策：全局 toggle（不是 per-session）位置在 Sidebar Quick Actions / 自动跟随 active session / **必须 YOLO mode 才允许**（safety gate）/ 单次硬限 30 step / 阈值 15min-4hr 可调 / macOS 系统通知 + Sidebar 🤖 indicator + 自主消息视觉变体。新 IPC: `enable_idle_autonomy` / `disable_idle_autonomy` / `idle_autonomy_fired`。~250 LOC。哲学定位：审批是出口不是围栏的延伸——给"信任 agent 自主跑"的用户一条透明 explicit 可控路径。Open：bridge 端 idle monitor 多 bridge 还是单 global？wall clock vs monotonic 时钟？初次激活要不要 confirm modal？ |
| 2026-05-13 | [UI 文案 i18n + brand 一致性 sweep + TopBar 标题菜单重构 + repo 重命名](./2026-05-13-ui-copy-i18n-and-brand-sweep.md) | 从「标题旁的 ⋯ 看起来像省略号」一个 UX 小坑出发，连出 10 个连贯决策：(1) TopBar 标题块变 dropdown trigger（caret-down）消歧 + 收编 session rename 入口；(2) i18n Plan B 框架（section header / brand 锚点保留英文、body / 菜单 / hint 全中文、代码 identifier 不动）；(3) session → 对话 / project body → 项目；(4) ApprovalForm 4 按钮 + 4 pill 全中文 + 「白名单」框架对齐（按钮 verb 和 pill verb 同源消除认知卡顿）；(5)「已通知 AI」否决「Galley / agent」（Galley 是 UI 壳不是收件方）；(6) Workbench → Galley brand 11 处 user-facing 漏网全收；(7) GA vs GenericAgent 分场景规则（长文展开 / 紧凑缩写）；(8) **GitHub repo 重命名 genericagent-workbench → galley** 连带 README + CLAUDE.md + SettingsAbout 3 URL + local remote。Rejected：换图标治标 / Pin 保留英文（「短 / 都懂」滑坡）/ 句子里保留 Project（混合语言最伤）/ repo 名 galley-workbench 二元品牌。Open：ApprovalForm icon 跟新 copy 框架对应不上 / SettingsAbout 还有 Links / Also by SubLabel 英文 / Re-run health check + Back 按钮还是英文。 |
| 2026-05-14 | [Project = 纯分组 · 回收 rootPath/CWD 绑定（GA memory/ 静默降级修复）](./2026-05-14-project-rootpath-rollback-ga-memory-coupling.md) | 讨论「闲时自主行动」时顺着 GA `请阅读自动化sop` 这条线发现一个潜伏的静默 bug：bridge spawn 时 `os.chdir(project.rootPath)` 让 GA 在「project 设了 rootPath」的 session 下读不到自己的 `./memory/...`——memory_management_sop 静默 fallback 到 "Not found. Do not update memory."，整个 memory 系统失灵但不报错。**修复 = 回收 cwd 绑定**：Project V0.1 从「分组 + cwd 绑定」改为「纯分组」，IDE 范式撤回 GA 范式（agent 自己评估、积累记忆、任意 cwd 工作）。方案 3 落地：保留 DB column / 类型字段 / read-write helper（forward compat），但 `useAppStore.spawnBridge` 那一行 `cwd: project?.rootPath` → `cwd: undefined`，UI 入口全隐藏（Create/Edit dialog 项目文件夹字段移除、Sidebar 项目卡 icon 不再按 rootPath 区分、filter banner 第二行 mono path 移除、ProjectsDialog path 行移除）；死掉的 toast「下次启动该 session 时会用 X 的目录」一并清。Rejected：方案 1 UI-only 隐藏（依赖关系脆）/ 方案 2 drop column（不可逆，legacy 数据有价值）/ bridge monkey-patch 双 cwd（违反非侵入宪法）/ 给 GA 注入 system prompt 让 agent 自己 cd（不解决 ga.py 硬编码 `./memory/...`）。Future path: IPC `set_cwd` 真正 live-sync 实现（[2026-05-13 deferred entry](./2026-05-13-project-cwd-copy-and-live-sync-deferred.md) 已有完整方案），等真实痛点信号再启动。同时确认：闲时自主功能本身按上一轮结论 defer 到 V0.2（YOLO 已就绪不再是 blocker，但 demand 信号未到）。 |
| 2026-05-14 | [Conversation polish marathon: streaming, /btw, Desktop Pet UX, fence filter](./2026-05-14-conversation-streaming-and-btw-marathon.md) | 同一天后半段一连 18 个 commits 把对话区做了全面打磨。**视觉**：user message 升级为 apricot 实色 callout（长对话回找提问的主视觉锚）+ ThinkingSummary 脱离 brand 色系到中性 surface + InlineToolPill 去 CheckCircle + AskUserBubble 规格对齐 user-msg。**置顶 placeholder 重塑**：「第 N 步 · 思考中···」从大 callout 块（~50px）降到 TurnMarker thinking 态单行（~16px），等待 ≥5s 自动显示 elapsed counter（`5-59s · 23 秒` / `60s+ · 已 1 分 23 秒`，每步独立计时）+ 全产品收掉 💭 emoji 例外。**导航**：⌥↑/⌥↓ 跳到上/下一条 user-msg + Settings → Shortcuts 同步。**流式输出启用**（核心改动）：`agent.verbose = True` 翻开 GA 的 per-token LLM streaming，代价是要 strip 一堆 verbose 装饰（`🛠️ Tool: ... 📥 args:` block / 5-backtick fences / `当前阶段：` prompt-induced preamble），全部加 cleanPartialContent 处理。**性能修复**：发现 verbose 副作用——工具 stdout 全程过线导致体验慢、UI 卡，新加 bridge `_FenceFilter` 状态机在 IPC 之前丢掉 fence 内 chunk（98/98 bridge tests + 7 fence test 全过）。**/btw side-question 完整实现**：bridge 用 `handle_frontend_command` sync 路径在 worker 线程跑（避开 agent.run() task queue 阻塞），新 IPC `SystemMessageEvent`、SystemTurn 类型、SystemMessageBubble 组件（黄色 AskUserBubble 同色家族）、Composer `/btw` prefix detection 让 stopMode 下也能发出去；exchange transient 不持久化（V0.2 候选）。**Desktop Pet UX 重塑**：sidebar Cat badge 显示 pet 附着位置 + menu label 二态化（active session 「关闭」/ 其它「桌面宠物」）+ 隐式迁移机制（store `pendingPetMigrationTo` 字段，detach→attach 串联避免端口竞争）。**Follow-bottom 修复**：sticky-bottom useLayoutEffect deps 扩到包含 `turns.length` / `pendingApprovals.length` / `pendingAskUser`，多步 run 每个 step 完成自动滚到底。**小修**：菜单「归入项目」→「加入项目」+ tool dispatch marker strip 兜底 + Composer stopMode 让 /btw 通过 + thinking placeholder 不再被 currentTurnIndex==null 拦住。Rejected: 始终显示秒数（机械）/ `/btw` install 路径（队列阻塞）/ stdout 当 prose collapsible（过度设计）/ persist /btw（V0.1 范围外）。Open: /rewind 4-commit 计划已设计未实施 / /btw 是否要持久化 / sys_prompt 自定义后 PHASE_PREAMBLE 精确匹配会漏。 |
| 2026-05-15 | [Release CI · Mac menubar · icon 4 轮迭代 · README screenshots](./2026-05-15-release-ci-menubar-icon-screenshots.md) | Y plan / A items 落地后下午-晚间一气补齐发版能力 + UI 收尾，14 个 commits。**Release CI**（`release.yml` tag-driven + `workflow_dispatch` 双触发 / 三平台 matrix macos-14 + macos-13 + windows-latest / pre-build typecheck+lint+cargo check gate / ubuntu-latest 收集 artifact + 创建 draft Release；`check.yml` PR + main push 同样三平台 gates 不 build artifact；`docs/release-workflow.md` 含 dry-run 路径 + 故障排查 + Intel runner deprecation 三 fallback）。**Mac menubar** 全套 6 submenu（Galley / File / Edit / View / Window / Help），Find + Toggle Sidebar 灰禁占位 V0.2 wiring；About 用 Tauri `PredefinedMenuItem::about` 弹原生 dialog 利用新 icon + version metadata。**App icon 4 轮迭代** 最终 JC 手调 1024 RGBA + squircle 烤进 alpha + 832/1024 Apple safe area。**关键发现**：macOS Big Sur+ 不自动 mask 第三方 icon，必须把 squircle 烤进 alpha 通道（Apple HIG 改了，Tauri T 占位符之所以是方角就是这原因）。**CI 实测抓 3 个 Win-only bug**：(a) pnpm version 没指定（packageManager 字段 fallback path 跟我们 `desktop/package.json` 位置错位）(b) `core:window:allow-internal-on-*` event permission 是虚构名 → 实际 event listening 走 `core:event:default` 已被 `core:default` 包含 (c) `window-shadows-v2::set_shadows` API 签名我又猜错——参数是 `&mut App` 不是 `&WebviewWindow`，返回 `()` 不是 `Result`。证实 `#[cfg(target_os = "windows")]` 代码 Mac 本地 cargo check 永远剔除，CI 是 must-have，每次改 Win 代码 push 前预算 1-2 轮 CI bounce。**README 6 张 screenshots** 临时 patch `demo.ts` + `useAppStore.ts` 的 `hydrateFromDB`（内存覆盖 sessions，不 touch SQLite），截图后 revert，落 `docs/screenshots/screenshot_0[1-6].png`。**SSH remote** 替代 OAuth PAT workflow scope（默认 Claude Code git token 不能 push `.github/workflows/`），配 `~/.ssh/config` + `id_rsa.pem` 一劳永逸。**JC 偏好** split arm64 + x64 Mac dmg（非 universal binary），管理两个文件换更小下载 + 用户视角清晰。Rejected: universal binary / Mac→Win cross-compile（Tauri 社区明确不支持） / `v0.1.99-test` 测试 tag（污染历史，workflow_dispatch 干净）/ ImageMagick 切多尺寸（Tauri CLI `pnpm tauri icon` 全包）/ icon v3-v4 再 AI 重生（JC 手调比 AI 快）。Open: macos-13 Intel runner 排队 (有时 6+ 分钟) 长期需要 fallback (drop / 本地 / 跨编译) / Win 11 圆角 / resize handle / Maximize 8px 溢出 / Snap Layouts 都留 Y6 Win 机 smoke / 6 张 screenshots 怎么进 README（next session）/ `_kaku_wrapped_ssh` zsh wrapper 错位待 cleanup。Next: dry-run release via workflow_dispatch (1 click) / Y6 Win 机 smoke / README 整合。 |
| 2026-05-15 | [Windows 发版 prep · Y 计划自绘 chrome + A 阶段杂项](./2026-05-15-win-prep-y-plan-custom-chrome.md) | Win/Mac dual-release 准备一整轮。**窗口 chrome 选 Y（fully custom）**：JC 为 Galley「精致工作台」定位选自绘 chrome 而不是回退原生 Win 标题栏。**Y 5 substep 全 landed**（独立 commit + Mac 验证 + 三层 Mac 安全门）：Rust setup hook 关装饰 + `window-shadows-v2` 阴影（target-specific Cargo dep，Mac 二进制零 Win 代码）/ `lib/platform.ts` UA-sniff isMac+isWindows / TopBar OS-conditional layout / `WindowControls.tsx` 三按钮 + Win-scoped capability `platforms: ["windows"]` / 双击 toggleMaximize（`isWindowActionTarget` walk-up DOM 排除按钮）/ `onFocusChanged` 失焦 desaturate。**Snap Layouts 跳过**（要写 WM_NCHITTEST Rust 等真痛点再说）。**A 阶段杂项**：A1 NSIS bundle target + currentUser installMode / A2 Python OS-aware（`python3` Mac, `python` Win）/ A4 `formatShortcut("Mod+K")` helper + 6 site 迁移（Mac 字符串 byte-identical）/ A5 `docs/windows-build-checklist.md`（25 项 smoke checkbox + 已知坑）/ A7 `EXAMPLE_GA_PATH` 常量（Mac `~/Documents/...` 不变，Win `C:\Users\你的名字\...`），3 个 site 迁移。A3 复盘后是 no-op（onboarding-tutorials.ts 早就把 `mkdir`/`md` 双版本写进去了）。Rejected: 方案 X 回退原生 chrome / `@tauri-apps/plugin-os`（三件套过重，UA 同步等价）/ literal Win 11 #c42b1c 红色（脱离 token）/ 自绘 restore icon（CopySimple 先用着）/ `%USERPROFILE%` 路径示例（不展开）/ demo.ts fixture OS-aware（被 prefs 覆盖触发不到）。Open: Win 11 圆角是否自动应用 / resize handle 抓握宽度 / Maximize 8px 溢出 / `onResized` 全路径覆盖 / Win+Arrow 跟我们 toggleMaximize 是否冲突——全部留给 Y6 Win 机 smoke test。Next: 借 Win 机器跑 25 项 checkbox + 出 `.exe` 验证；中期 GitHub Actions matrix CI 替代「借机器」流程。 |
| 2026-05-15 | [Vision pivot · workbench → local agent team orchestrator (dual-native)](./2026-05-15-vision-pivot-to-orchestrator.md) | 8 轮 brainstorm 产物：产品定位从「GA 的本地桌面工作台」reframe 为「本地 agent team orchestrator，dual-native for human and agent」。Galley CLI 一等公民 + Supervisor Agent 概念 + localhost-only 架构原则 + agent-api 公开契约 + Galley Core 权威层迁 Rust（路径 B 重构，10-12 周）。术语表（Supervisor / session team / Galley Core / runner/）+ CLI 命令 surface + 发包策略（sidecar + discovery file）+ 6 条 agent-first 输出规则 + 路线图 v0.2 Windows → v0.5 全套重构（同日 push back 后从 v1.0 改为 v0.5，详 devlog §D12.1）。Rejected: CEO mode 命名 / MCP 主路径 / approval routing 当核心 / 路径 A 中继 / long-lived branch / 远程 auth / 存 supervisor 对话。Next: PRD v0.3 草稿 + bridge-owner prototype spec 同时落地，待 JC review 升格。 |
| 2026-05-15 | [v0.1 prod-build dogfood fixes（Onboarding routing · Python probe · bridge stderr · Settings revisit）](./2026-05-15-v0.1-prod-dogfood-fixes.md) | 第一次跑 `pnpm tauri build` 出 .dmg 真启动，30 秒内暴露 4 个独立 bug：无 Onboarding（fresh 用户 never reaches it，缺 `setScreen("onboarding")` 路由）/ LLM 列表是 demo fixture / 发消息无反应（DEMO_GA_CONFIG.python=`python3` 在 launchd PATH 下解析到 macOS 自带 3.9.6 无 GA deps，bridge import 即崩静默死掉）/ Re-run Health Check 是 `console.info` 占位符。**修复**：(a) hydrateFromDB 检测无 `ga_config` pref → `setScreen("onboarding")`；(b) 新 `lib/python-probe.ts` 预注册 10 个候选 Python 路径（Tauri capability `cmd` 不支持 regex validator，只能枚举），运行时 spawn `sys.path.insert(0,gaPath); import agentmain` 逐个验证，第一个 ok 胜出存入 `gaConfig.python`；(c) bridge stderr 加 per-session 滚动 buffer，abnormal close 弹 toast 显示最后 3 行（含 spawn-error 路径）；(d) Onboarding 双层 drag region（外层 `data-tauri-drag-region` + 内层 `="false"` 让文本可选 + 按钮可点）；(e) Settings → Health Check 选 Option A 重构（按钮接 Onboarding StepHealth revisit 模式跳过 Welcome/Attach，Back→「取消」+ 进入→「返回 Settings」+ 恢复 screen + 重开 Settings dialog）。**关键 mid-execution 修正**：初版 probe 用 `import anthropic, openai`，实测 JC 机器所有 Python 都 fail——查 GA llmcore.py:1 发现 GA 用裸 `requests` 直接调 LLM endpoint，**根本不引入 anthropic / openai SDK**，改成 `import agentmain` 校验真实 import chain。**删旧死代码**：`rerunPythonProbe` store action / `RuntimeInfo.healthChecks` 字段 + demo fixture（之前那张静态表永远显示 5 项 success，本质在撒谎）。Rejected: Rust-side custom spawn 命令（解决任意 Python 路径，~200 行 Rust，推 V0.2/B refactor）/ bundle Python sidecar（+50-100MB）/ Settings 内联复刻 cascade（双份维护陷阱，本 session 就重现了 divergence）/ stderr 全量 toast（spam）/ revisit 走 Welcome（重）。Open: conda/pyenv/asdf/uv 用户的 Python 不在预注册名单（V0.2 Rust spawn 解决）/ Probe 不自动 TTL（venv 后期坏掉只能用户手工 Re-run）/ `python-missing-anthropic` tutorial id 现在 misleading 但重命名属 cosmetic deferred。 |
| 2026-05-15 | [GA Baseline 升级 6bb3104 → fc6b5ad（零接口表面变化）](./2026-05-15-ga-baseline-upgrade-6bb3104-to-fc6b5ad.md) | v0.1 release-readiness 同一 session 顺手把 baseline 升到 upstream/main HEAD。13 个 commits 全审完：`agent_loop.py` / `agentmain.py` 0 行；`ga.py` 4 行（仅 maxlen 阈值 20000→15000 / 150→100）；`llmcore.py` 54 行（`trim_messages_history` 签名 `(history, context_win)`→`(history, sess)` 但**只在 GA 内部调用** + `compress_history_tags` 加 `interval=5` 默认参 + `BaseSession` 按模型选 default context_win）。bridge **零代码变更**——只读 `agent.llmclient.backend.history` 列表语义保持。测试矩阵：bridge/tests/ 106/106 在 fc6b5ad（新）+ 6bb3104（旧）两端都通过。JC 本地 GA main 同步 fast-forward 到 fc6b5ad 保持 Settings 「GenericAgent 版本」卡片 `已对齐 baseline`。 |
| 2026-05-15 | [v0.1 ship · CI Intel queue fallback · tiered release pattern · macOS terminology](./2026-05-15-v0.1-ship-and-ci-fallback.md) | Galley v0.1 公开发布——**macOS RC + Windows Alpha 两个独立 release**（dual-tier strategy）。dry-run CI 验完 push v0.1.0-rc.1 tag → macos-13 Intel runner queue 卡死 → **manual fallback**：取消 stuck CI + 下 macos-14 aarch64 artifact + 本地 build x64 + `gh release create --draft`。Win 走 v0.1.0-alpha.1 tag 同样手动。GitHub API 不允许 prerelease 标 Latest → Mac RC 摘 prerelease flag 换取 sidebar widget visibility（title "(Release Candidate)" 文字 + notes 保留 tier 信号）。**macOS vs Mac 用词约定**：macOS = OS / Mac = 设备，按 Apple 惯例。配套 SOP 更新 [release-workflow.md](../release-workflow.md) 加 4 新 section（Tiered release / Manual fallback / Latest+prerelease 限制 / Announcement templates）+ [CLAUDE.md](../../CLAUDE.md) 加 Platform terminology section。Rejected: 等 Intel queue 到底 / 单一 release 包 3 平台 / 保留 prerelease 接受 sidebar 空 / 通告 template 入库（每次内容不同，只入结构）。Open: 何时第一个非 prerelease release（看反馈 2-4 周）/ Intel runner 真下线后用方案 C 跨编译 / code signing v0.6+ 触发节点（100+ downloads + 多非 JC 反馈者）。 |
| 2026-05-15 | [v0.1.0-alpha.2 Windows attach hotfix（fs:scope · mixed-separator joinPath · Settings GA Path editable）](./2026-05-15-v0.1-alpha.2-windows-attach-fixes.md) | Win Alpha 用户群两个独立的 attach-time bug 在同日先后报上来，挂在「mac dogfooders 凑巧都把 GA 装 `~/Documents/`」这个 alignment 上一起暴露：(a) GA 在 `Documents` 下的用户——picker 路径合法但「错误路径」红字，手动加 `\` 才过，根因 `joinPath` 硬编码 `/` 拼出混合分隔符 `C:\Users\foo\GA/agentmain.py`，Tauri plugin-fs 处理不一致；(b) GA 在 `D:\projects_2026\GenericAgent` 的用户——Health Check 第一行就 fail，根因 `tauri-plugin-fs` 的 `fs:scope` 默认只放行 `$HOME/$DOCUMENT/$DESKTOP/$DOWNLOAD` 四个 glob，D 盘根本不在 scope 内 → throw → catch 当 not-found。**修法**：(a) `joinPath` async 化走 `@tauri-apps/api/path.join` + `resolvePath` 新增 normalize 步把 `~`-expand 后的路径过一遍 `path.normalize`；(b) src-tauri 加一行 Rust Tauri command `path_exists` 绕开整个 plugin-fs，JS 端 `fsExists` 改 `invoke("path_exists",...)`。**附带**：Settings → Runtime GA Path field 改可手动输入（不只 picker），300ms debounce validateGAPath + Enter/blur commit + Esc revert + picker `onMouseDown.preventDefault()` 防 blur-commit 抢跑。**CI matrix 永久 drop macos-13**：JC Intel Mac 自 build 兜底；macos-13 GitHub deprecation 提前撤；alpha.2 起 CI 出 2 artifact（aarch64.dmg + x64-setup.exe）。Rejected: 「.windows-patch」tag 后缀只发 Win 包（Mac 用户 GA 在 ~/Documents 之外也受影响）/ `fs:scope` 全开 `**`（Tauri Windows glob 跨 drive 行为未文档化 + 打开未来 plugin-fs read/write 全面）/ 单次 skip Mac Intel 不改文档（alpha.3 还要 revert）/ defer Settings editable 到 alpha.3（已 typecheck 干净）。Open: MessageActions writeTextFile 仍走 plugin-fs + fs:scope（picker-gated 但 D:\ 保存仍会静默 fail）/ python-probe 不识别 conda/pyenv/uv/asdf（V0.2 Rust spawn 解决）/ 无自动测试覆盖 fs:scope hole 这类 regression。Next: 6 个 commit + tag v0.1.0-alpha.2 + push（CI ~15-20 min）+ Mac arm64 + Win 借机器 smoke test + tiered release notes publish。 |
| 2026-05-15 | [Onboarding & empty-state polish · YOLO default · button system · v0.1 Mac-only 决策](./2026-05-15-onboarding-empty-state-yolo-button-polish.md) | 两个 commits（[72ce4d3](https://github.com/wangjc683/galley/commit/72ce4d3) + [143f44c](https://github.com/wangjc683/galley/commit/143f44c)）覆盖 8 大件。**Conversation 步骤渲染**（commit 1）：TurnMarker 整行可点击 + 条件 chevron 展开 DetailPanel（thinking + 前言并入），新 TurnTicker 在 streaming 中显示前言最新段 line-clamp-3，InlineToolPill 双区（左 icon + 中文名 + arg preview，右 mono GA name + chevron）。数据层：migration 005 加 `messages.preamble` + `extractPreamble` 反向抽取（strip 所有结构 tag 取剩余 prose），`isFinalTurn` 防护避免 final answer 双渲染。**Onboarding 教程系统**：5 个手写 fix-it 片段（download-ga / wrong-directory / mykey-setup / assets-missing / memory-info）+ TutorialModal Radix Dialog + HealthCheckCard 把 actions 扩到 warning state + StepHealth 加「重新检查」按钮 + StepAttach 失败时显示 tutorial 按钮 + 「Attach」全部改成「接入」。**Welcome 双卡片入口**：删 3 条 bullet + footer + 「开始」按钮，加两个 ModeCard（Mode 1 灰禁「敬请期待」/ Mode 2 active「Galley 不会修改你的 GenericAgent」），副标题去句号。**Empty State 重塑**：删大标题「你想做什么？」+ placeholder 升「今天交代什么？」承担邀请、4 个 chip 按钮降级成 prose 提示（italic serif 12.5px ink-muted 居中，无 border / bg / icon，hover 只变色）、新 prompt 跨 4 个任务形态。**LLM display name heuristic**：bridge `_simplify_llm_name(raw, model)` 加 `name != model → 原样返回` 规则，用户 mykey.py 显式 `name` 字段终于被尊重；新 `_safe_get_model` defensive helper（MixinSession 没 .model / BADCONFIG dict）；+8 测试 → 106/106。**YOLO 默认 ON + 首次声明 modal**：v0.1 GA 重度用户默认开 YOLO，但首次进 MainView 弹阻塞 modal 强制 acknowledge；ESC/overlay/X 全禁，只能点「改回审批模式」或「知道了」；`yoloIntroSeen` pref 只对真新用户翻 false（现有 dogfood 用户跳过避免「title 说默认 ON 但实际 OFF」尴尬）。**按钮系统**：Audit 出 4 个系统问题（primary 颜色二分裂 / 尺寸 3 种 / secondary 文字色 3 种 / disabled 行为乱），新 `components/ui/button.tsx` 5 variant × 3 size，primary canonical color 选 `bg-ink`（克制，非 brand-strong 杏色），轻量迁移 4 个最显眼 site（CreateProject / EditProject / Sidebar 空 project CTA / YoloIntroDialog）。**v0.1 Mac-only 决策 + Windows v0.2 计划**：Mac-only 释放避免「未测平台仓促发」风险；v0.2 通过 GitHub Actions CI build NSIS `.exe`（不 cross-compile，不 VM 长期维护）；Mac-side prep work 6 项（NSIS bundle / Python OS-aware default / OS-conditional 教程命令 / 键盘 mod-key 抽象 / joinPath / Windows checklist）推到下次 session，必须 Mac-backward-compatible。Rejected: 自动展开当前步 / `<thinking>` 单独 callout / 第一性原理删 4 chip / Onboarding 加 YOLO 偏好步 / YOLO banner / 按钮全代码库迁移（B 重型）/ Primary 用 brand-strong / chip prefill 模板 / joinPath 改 Tauri path API（强行换）/ 借 Windows 通宵 build。Open: Windows prep 何时启动 / 键盘快捷键 audit 精细度 / OS detection runtime vs Tauri async / CI workflow 落地时机 / destructive-soft 推广面 / PRD §11.5 YOLO 默认值更新待补。 |
| 2026-05-18 | [Bridge-owner prototype · 17/17 PASS · GO for B1](./2026-05-18-prototype-go-for-b1.md) | 单一 session 把原计划 2-3 天 throwaway prototype 全部 17 项 checklist 推完，**GO for B1**。Lifecycle (L1-L5) 全过 — spawn ready 430ms · 3并发 341ms（比单 spawn 快，Python startup 共享 cache）· external `kill -9` 检测 3.48ms · panic+unwind drop SIGKILL 干净；Stdin command (C1-C3) — set_llm RTT 1.7ms · 5 fire-drain 无死锁；Stdout subscriber (S1-S4) — 双订阅（broadcast + UnixListener）内容一致 · S3 100 events 4548/s · S4 subscriber disconnect 互不影响；Stress (X1-X2) — 10k events sustained 4498/s linear scaling no OOM；Performance (P1-P3) Approach B（Rust 单边量化，TS 严格对比推 B1 dogfood）— RTT p99 614µs · 4684/s · **300s 内存 +0.4 MB，t+111s 后 RSS plateau 3+ 分钟** 是 zero-leak 最强信号（spec 阈值 50 MB，实测 0.4 MB，125 倍 cushion）。设计 call：独立 tokio binary（spec 自相矛盾，broadcast + lifecycle 才是假说，Tauri emit 是 commodity）。Open: graceful shutdown 慢（~2.5s/bridge）记进 B2 设计 TODO。新增 invariant I11：Cargo `panic = "unwind"` 必须保留（L5 PASS 的前提）。`BridgeProcess` (registry.rs 145 行) 是 B1 runner_manager 模块的 source pattern，pre-subscribed receiver 是 load-bearing 实现细节。4 commits: [8d4769c](../../) scaffold · [c22e6c1](../../) L5+C+S+X · [79e1f0a](../../) P+verdict · [8fb66ba](../../) 300s plateau。 |
| 2026-05-18 | [GA baseline upgrade fc6b5ad → b063518](./2026-05-18-ga-baseline-upgrade-fc6b5ad-to-b063518.md) | v0.1.1-alpha.1 发版前按「准备发版」触发器对齐基线：上游 49 提交（本项目基线追踪以来最大单跳），主体是 TUI v2 主题 / hive worker 修复 / 新 slash 命令,均不触 bridge 层;按 Baseline Upgrade Workflow 逐步走完,契约面无断裂。 |
| 2026-05-18 | [v0.1.1 · Bundled Python:zero-config attach + Phase 0/1](./2026-05-18-v0.1.1-bundled-python.md) | Issue #1(Windows uv 用户 `.venv/Scripts` 布局不被识别)暴露结构性问题:静态 allowlist 追不上 Python 管理器宇宙。决策转向随 app 捆绑 CPython:Phase 0 POC 验证 + Phase 1 四个 commit 落地,UI 简化 / CI 分架构 / ship 拆后续 session。 |
| 2026-05-19 | [B2 Bridge ownership 迁 Rust · 完成（代码 + 文档）](./2026-05-19-b2-bridge-ownership-complete.md) | 单一 session 把 B2 全 7 个 milestone 代码层推完，dogfood 1-week 留下次 session。**M1 RunnerManager scaffold** (`59176a2`) `core/src/ipc.rs` 镜像 runner/ipc.py 17 events + 12 commands；`runner_manager/{mod,process,manager,error}` 把 prototype `BridgeProcess` 升 production：typed `BroadcastItem` channel、HashMap-by-session + LRU eviction（active + agent_running 双重保护）、8 行 stderr 滚动 buffer、3 个 typed error enum；35 新测试。**M2 bridge.ts thin shim** (`82ffedc`) 5 Tauri command 包 RunnerManager + 起 per-session emit task；`Arc<RunnerManager>` Tauri app state；GUI bridge.ts 函数签名 byte-identical 但 body 全换 invoke + listen；CI check.yml 加 cargo test 双 matrix。**M3 socket listener** (`19e33d6`) `socket_listener.rs` (~430 行) Unix domain socket `$TMPDIR/galley-$UID.sock` (0600 + hand-rolled `extern getuid`) 跟 Windows named pipe `\\.\pipe\galley-$USERNAME`；函数级 cfg 而非 enum 统一；NDJSON 协议 + 8 个 stable error discriminant；race detection 200ms try-connect + stale unlink；`SocketGuard` Drop unlink；90s idle timeout；19 新测试。**M4 CLI session send/watch + M5 origin migrations 006/007** (`2e6157b`) `GalleyApi::send_message` trait method + `SqliteGalley` impl 持久化 origin 三元组；socket `session.send` 返回 `{message, dispatch}` 双字段 / `session.watch` 通过 `DispatchResult::Stream` 长连接 push NDJSON 事件；CLI `session send/watch` subcommand + 2 个 exit-4 测试；`OriginVia {Manual,Cli} → {Gui,Cli,Supervisor,System}` 跟 SQL CHECK 对齐；invariants §I3 修正（B1 没用 006-009，B2 接 006）。**M6 agent-api.md increment** (`1575044`) §2A "Transports" / §5.5a-b commands / §6A Shared types / §6 error envelope split / §1 + §7 stability 包含 socket wire。**Dogfood 修 3 个 bug** (`b087f22`) `availableLLMs` serde camelCase acronym quirk → 显式 rename + regression test；`gaConfig.python` capability alias 不翻译 → `resolvePythonPath` 加 `findCandidateByAlias`；spawn `io::Error` 不带 path → wrap 进 detail message。**Tests**: 83 全过 (45 lib + 9 + 7 + 8 + 13 integration)。**13 decisions** (B2-I1 signature lock / 5 Tauri cmds / pull stderr / 函数级 cfg / hand-rolled getuid / block_on setup / dispatch 双字段 / watch --from 推 B4 / 006/007 不跳号 / OriginVia 扩展 / trait persist-only socket dispatch / manual sync IPC 不 codegen / B3 playbook 同 session 升格)。**Open**: dogfood 1-week 留下次 session · perf gate vs prototype baseline · socket 0600 TOCTOU hardening · `--from` backlog · CLI happy-path integration test。 |
| 2026-05-19 | [B3 M1 完成 · slice 设计 + ADR + Rust emit catalogue（0 代码改动）](./2026-05-19-b3-m1-design-complete.md) | M1 paperwork 阶段全 10 sub-task 串推完，~3.5h（G1 4-6h 预算 1.5× under）。3 artifact：(1) [b3-slice-mapping.md](../archive/refactor/b3-slice-mapping.md) — 字段-级 inventory，89 distinct items（32 字段 + 57 action）分到 5 slice，含 12 个 active-session projection mirror 在 M3-M5 retire 的清单 + 9 个 dead-after-B3 module-level symbol。(2) [b3-slice-adr.md](../archive/refactor/b3-slice-adr.md) — 11 AD 覆盖 5 slice 划分（含 prefs 独立成第 5 slice 理由）/ activeProjectFilter / yoloIntroSeen / conversationWidth / agentRunning / userSubmitTick 边界判断 / module-level 9 symbol 全删 / DAG 单一 hot edge (prefs → runtime) / RESOLVED 字段 (Zustand / 16ms batch / selector ≤ 2 layers) 复述。(3) [b3-rust-emit-catalogue.md](../archive/refactor/b3-rust-emit-catalogue.md) — 5 新 domain event spec（sessions-updated / messages-appended / projects-updated / prefs-updated / runtime-updated）+ 3 沿用 + 跨 channel ordering / 16ms streaming batch / initial state 走 invoke 不走 emit / migration sequence M3-M6 增量。**关键发现**：(a) Rust 端 emit domain event 不 raw runner-event 给 GUI = IPC 解释逻辑中央化（[CLAUDE.md 路径 B 不可逆](../../CLAUDE.md) 在 TS 端落地）；(b) `getBridgeClient` 0 个外部 callers (grep verified)，AD-07 module-level 删 100% 干净；(c) sessionsStore / messagesStore 估计 550 / 600 行，**接近** B3-I5 600 行硬上限，M5 实施时预备子文件拆分。Rejected: 4 slice (合并 ui+prefs) / filter 挂 ui / agentRunning 挂 runtime / getBridgeClient 留 @deprecated / 5 emit 合一 / schemars-derive codegen / 32-50ms batch (streaming delay 肉眼可见) / subscribe-since-timestamp race protocol。Open: O5-O10（filter / mock fixture / agentRunning / userSubmitTick / gaConfig listener / invoke-listen race）全部留 M3-M5 dogfood 时复核。Next: M2 T2.1 (uiStore)，启动前需 M2 启动门 (scenarios + perf baseline)。 |
| 2026-05-19 | [B3 M3 完成 + 4 个 B2 latent bug 修复 + perf baseline 落地](./2026-05-19-b3-m3-complete.md) | 单一 session 9 commits 叠加产出。**Perf baseline 框架 + 实测** (`7c648fa`)：5 metric 完整 spec + gate 规则 codified；P3 (CLI RTT 13-15ms) / P4 (cited prototype 430ms cold) 自动测；JC 配合驱动真 LLM 实测 P1=3.15s short / 5.36s long、P2=1.42 ev/s。测量过程 3 finding：script `readline()` block bug → `select.select()` 修；GA wire format `data.kind` 嵌套；GA emit ordering surprise 长 prompt 时 `turn_progress` 早于 `turn_start`，P1 marker 必须 `first_real_delta`。**4 B2 latent bug fix**：(1) `4e7a6e6` CLI-origin user message GUI 不显（socket_listener 没 emit user-message-persisted → 加 Tauri event + appendUserTurnExternal）；(2) `9c36f42` 每 session LLM 选择不持久化（schema 早有 llm_index 但 persistSession 硬编码 null，纯接线）；(3) `2779baa`→`d6a096f` LLM picker flash（第一次只修顶层 projection 不够，applyRuntimeUpdate 又回退 DEMO，真根因是 _runtimes[id] 本身 DEMO，pre-seed 解决）；(4) `317e816` picker click 在 spawning 期间 silent drop（gate `connected` 太严，放宽 `connected || spawning`）。**M3 sub-plan** (`3f76cf4`) 318 行落地：拆 M3a (LLM concerns ~300 LOC) + M3b (bridge lifecycle ~500 LOC) 不单 commit；playbook T3.5「thin wrapper」描述被推翻（spawnBridge 140 行 + 复杂 onClose）；cross-store onClose transitional pattern 用 inline `TRANSITIONAL (M5)` 注释；6 risk + 7 verification gate（含验证 4 B2 fix 不退化）。**M3a 实施** (`cc22aa4`) 新 `gui/src/stores/runtime.ts` ~300 LOC（byId map + 全局 pendingLLMIndex / petAttachedSessionId / runtimeInfo / _warmupComplete + 新 cachedLLMs / cachedLLMDisplayName fields + ensureRuntime centralize pre-seed）；useAppStore.ts 净 delete ~290 LOC；3 TRANSITIONAL annotation。**M3b 实施** (`22e6590`) bridgeStatus / bridgeError / bridgePid 迁 byId；9 个 module-level symbol 从 useAppStore 全清干净（runtime.ts 内部 private 重生）；spawnBridge 140 行迁过来，cross-store onClose（bridge fields → runtimeStore.setState，conversation cleanup → useAppStore.setState 加 TRANSITIONAL）；_enforceLRUCap 读 useAppStore._runtimes 同 TRANSITIONAL；`lib/sessions.ts::deriveSessionStatus` 加 `bridgeStatus?` 参数。**总计**：useAppStore.ts 从 ~2700 → ~1700 行（-1000 LOC 净 delete），runtime.ts ~570 行。Rust 76/76 ✓ + TS typecheck + lint 0 warning。Rejected: M3a 同时迁 spawnBridge / Rust 端加 runtime-updated event / replaceLLMs 留 deprecated shim / flash fix 只修顶层 / picker click gate 完全去掉。Lessons: playbook 描述需重读代码验证；flash UI bug 两层 root cause；B2 latent bug 进 M3 前一次扫清值得。**Next**: M4 sessionsStore — 必须动 Rust 加 18 trait method（独立 commit），fresh session 重开。 |
| 2026-05-19 | [B3 prerequisites · 1 周日历仪式改成事件驱动 + 双层 gate](./2026-05-19-b3-prereq-relaxation.md) | B2 ship 同日 JC 想推 B3，发现 playbook 升格时写的「B2 完成后 dogfood 1 周稳定期」prereq 跟 B2 完成 devlog 自己的话（"the dogfood period is an empirical confidence-building step, not a gating contract"）+ CLAUDE.md「事件驱动，非日历驱动」原则双重冲突，且 T1.1（pure paperwork）跟 M2（动 frontend 代码）风险量级差 100×。**Decisions 5 条**：(1) 拆 M1 启动门（轻，scenarios 列表先写）+ M2 启动门（重，scenarios 真跑过签字 + perf baseline 测好）双层 gate；(2) 删「1 周」时长 gate 换 scenarios + 签字事件驱动 gate；(3) scenarios 列表前置到 M1（既给 B2 dogfood 用也给 B3 milestone-end regression suite 用，**T1.1 前先写**）；(4) perf baseline 推到 M2 gate（M1 paperwork 不动 perf，基线测了用不上）；(5) playbook + cursor + devlog 同一 commit ship（规范本身可疑就改文档形成 decision provenance）。**Rejected**: 删全部 dogfood prereq / 单层 gate「1 周」改「3 天」/ T1.1 当作「prep work 不算 B3」绕 gate / 整体放弃 dogfood（B2 dogfood 已抓 3 个 tests 抓不到的 bug）。**Open**: scenarios 列表覆盖度定义 / perf baseline 测法 / M2 启动门触发后 M1 是否要 revise。Next: T1.1 立即可启动，但先 force 写 scenarios 列表。 |
| 2026-05-19 | [B3 M5 完成 — messagesStore 抽离 + active-session projection retire](./2026-05-19-b3-m5-complete.md) | 单 session 把 M5 sub-plan + implementation 串完（破 sub-plan 写的「M4b dogfood + fresh session」启动门，JC explicit override）。**Single commit M5 决策落地** (`f7fc4e7`)：sub-plan §2.2 拒绝的 4 候选 split seam 全部失败 → 单 commit 1052/-1160 LOC 改 9 文件。useAppStore.ts 1431 → 465 行 (-966 LOC) — 最后一个 per-session authoritative slice 抽离，B3 后 useAppStore 只剩 prefs + hydrate orchestrator 等 M6 拆。**字段迁移 (9)**: turns / pendingApprovals / agentRunning / currentTurnIndex / inFlightContent / approvalDecisions / pendingAskUser / turnIndexOffset + global userSubmitTick。**Actions (14 + 1 orchestrator)**: conversation write path (appendUserTurn / Ext / SideQuestion / appendAgentTurn / appendSystemTurn / setAgentRunning / setCurrentTurnIndex / appendInFlightDelta / clearInFlightContent / setPendingAskUser / clearConversation) + approval (add/remove/recordApprovalDecision) + restoreSessionTurns。activateSession 搬 sessionsStore (Option B — active id owner) 不留 useAppStore。**4 cross-store transitional stub 全 fix**：M3b onClose useAppStore.setState 3-field → messagesStore.clearStreamingOnBridgeClose / M3b _enforceLRUCap agentRunning probe → messagesStore.byId / M4b clearSessionRuntime helper → clearSessionMessages (dynamic import dropped, now static) / M4b applyRuntimeUpdate driver → messagesStore internal patchMessages + fireSessionMirror 直调 sessionsStore.applyDerivedFromRuntime。**deriveSessionStatus 签名换** 结构化 MessagesView (pendingApprovals + agentRunning) 不绑 store 模块 — lib/sessions.ts 不反向依赖 stores。**Sub-file split per G11**: messages.ts 第一遍 702 行超 600 上限 → rowsToTurns + safeParseJsonArray + previewFromContent 抽到 `gui/src/stores/messages/rowsToTurns.ts` (123 LOC pure functions)，messages.ts 落 584 行 ✓。**EMPTY singletons**: EMPTY_TURNS / EMPTY_APPROVALS / EMPTY_DECISIONS / EMPTY_MESSAGES module-level Object.freeze + double cast (`readonly never[]` 不兼容 `Turn[]`)，React 19 strict-mode getSnapshot 稳定 reference 关键。**Cross-store import**: M4b 的 `clearSessionRuntime` dynamic import pattern 改成 static import — runtime.ts 早就 static import sessionsStore，Vite ES module cycle 处理只要 module-top-level 不触读就 OK，pattern 已经允许。**Rust 0 改动**: B3-I4 守，T5.2 messages-appended event + T5.3 16ms batch 推 B4 (N7 perf 1.42 ev/s React 无压力)。126/126 Rust tests + TS typecheck 0 error + lint 0 warning + cargo check clean。**23 call site swap**: App.tsx 12 + ipc-handlers.ts 11；MainView prop drilling 不变 (App.tsx 顶层 selector 改)。Rejected: M5a/M5b 拆分 (B-1/B-2/B-3/B-4 全否) / Rust 端引入 batching / activateSession 留 useAppStore / React Context 替代 Zustand / userSubmitTick 迁 uiStore / readonly EMPTY type / lib/sessions.ts 反向依赖 store / dynamic import retained。Open: T5.16 dogfood 7 cluster + V5/V6/V7 高 severity gate (R3 auto-scroll regression + R4 turnIndexOffset PK correctness) 留下次 session；M6 prefsStore scope re-eval（useAppStore.ts 465 行 90% prefs，hydrate orchestrator 100 行 + setters 100 行；M6 T6.6 可能彻底删 useAppStore.ts）；streaming perf re-measure；sessions.ts 1054 行子文件拆分时机；MessagesView 抽到 types。Next: JC dogfood → M6 sub-plan → M6 impl → B3 acceptance + tag。 |
| 2026-05-20 | [B3 完成 · useAppStore 拆 slice + 改订阅 Rust event](./2026-05-20-b3-store-slice-complete.md) | M1-M6 全 ship + M7 acceptance + tag `b3-complete` 同 session 收尾。B3 整体跨 6 session、2 day calendar (playbook estimate 3-4 weeks could slip to 5-6)，21× faster than estimate。**最终 store layout**: ui (66) + runtime (~680) + sessions (~1160) + messages (584 + rowsToTurns 123) + prefs (378) + lib/hydrate.ts (127). useAppStore.ts (2858 行 B2 ship) 整文件删除. **Estimate miss drivers** 5 条: sub-plan paperwork-first / B3-I3/I4/I5 三条硬规则 / single-commit ship pattern / TS strict + cargo check + ESLint 三重 gate / JC override pattern 吸收 paperwork-adjacent risk。**B3-I1 dogfood-1-day-per-slice rule** event-driven override — paper 上是 "每 slice 1 天"，实际 "authoritative state 改动后 dogfood + paperwork-adjacent 允许 override"。**Lessons 8 条**：sub-plan-first / single-commit > 拆 commit / 编译期 gate 抓 80%+ regression / JC override pattern / CLAUDE.md "no PR-narrative in comments" enforced / sub-plan +30% buffer for docstring porting / B3-I3 hard enforce / B3-I4 几乎守住。Open: cosmetic sub-file split / DevTools exposure 不一致 / perf baseline post-B3 / B4 phase-switch dogfood window event-driven。Next: B4 playbook stub 升格 (fresh session paperwork) → sub-plan-then-impl chain → v0.5 milestone 2-3 周内 ship。 |
| 2026-05-19 | [B3 M4 完成 — sessionsStore + Rust GalleyApi 17 trait methods](./2026-05-19-b3-m4-complete.md) | 单 session 4 commit 把 M4 全部 sub-plan + M4a + M4a addendum + M4b 三阶段推完。**Sub-plan ship** (`0943c91`) 348 行 mirror M3 结构：playbook「18 method」改 16（12 session + 4 project），setActiveSession/activateSession/setActiveProjectFilter 是 pure display state op 不需要 trait method；M4a (Rust trait + tests, 独立 commit) + M4b (frontend sessionsStore 抽离, fresh session) 两阶段拆分；R1-R8 risk + 7 verification gate。**M4a ship** (`eca7d65`) 16 trait method 落地 GalleyApi + SqliteGalley 实现 + Tauri commands + 47 个新 cargo test (76 → 123)；map_constraint_err shim 把 sqlx Database error 映射成 InvalidArgs；truncate_summary Rust/GUI 80 字符一致；agent-api §8A 增量 schema 表。**M4a addendum** (`35f0e78`) 新增 set_session_llm trait method（M4b 实施时发现 runtimeStore.replaceLLMs 用 persistSession 全行 upsert 持久化 LLM 选择，M4b 删 persistSession 就 dangling）+ 3 test。**M4b ship** (`ad342e9`) 新 sessions.ts 1054 行（state + 17 action + mock fixtures + cross-store coordination via dynamic import 避开 module-eval cycle）；useAppStore.ts 2362 → 1431 行 (-931 LOC)；lib/db.ts 883 → 607 行 (-276 LOC，删 persistSession / persistProject / deleteSession / deleteProject / loadSessions / loadProjects / sessionFromBrief / sessionFromRow / projectFromRow / persistableStatus / SessionBriefWire / DURABLE_SESSION_STATUSES)；activateSession 是唯一 stays-in-useAppStore action (orchestrator)；22 call site swap (App.tsx 14 actions + 4 state reads + ipc-handlers.ts + runtime.ts + sessions.ts)。**Cross-store pattern**: applyRuntimeUpdate → sessionsStore.applyDerivedFromRuntime / appendUserTurn → maybeDeriveTitle / runtime.mirrorSelectedLLMOnSession → setSessionLlm / clearSessionRuntime via dynamic import；3 处 TRANSITIONAL (M5) 注释 grep-able。Rust 126/126 + TS typecheck + lint + cargo check 全 clean。**Rejected**: M4a + M4b 单 commit / activateSession 迁 sessionsStore / patch_session 通用 endpoint / addendum 跟 M4b 同 commit / 保留 deprecated functions / sessions.ts 同步分子文件。**Open**: sessions.ts 1054 行超 B3-I5 600 行硬上限（flag M5 同步分子文件 per G11）/ M4b dogfood 1 天验证 / 35 项 scenarios formal walk 推下次 / M5 sub-plan 时 audit messages persistence 全 caller 避免 set_session_llm 类漏。**Lessons**: sub-plan audit 必须 grep 现有持久化 caller 验证 trait surface 覆盖完整 / `PRAGMA foreign_keys = ON` SQLite test setup must-have / distinct timestamp fixture 避免 ORDER BY 实现定义碰撞 / map_constraint_err 字符串匹配是 sqlx 0.8 局限。|
| 2026-05-18 | [B1 Rust core 骨架 + CLI 只读 · 完成](./2026-05-18-b1-rust-core-complete.md) | 单一 session 把 B1 全 7 个 milestone 推完。**M1** 目录重组 (`4ee23e3`) 178 文件 161 git-rename 检测 / desktop/src-tauri→core, desktop→gui, bridge→runner, 新 cli/。**CI 热修** (`ff878b1`) 修 pre-existing Check workflow python-bundle 路径验证失败（v0.1.1 起）。**M2** Cargo workspace + crate `desktop`→`galley-core` + `GalleyApi` trait 6 read methods + 全套数据类型 (`d79558a`)。**M3** sqlx 0.8 SQLite reads（复用 tauri-plugin-sql 的同版本，零新 transitive）+ 12 in-memory tests + 第一个 Tauri command `list_sessions` (`9f6b369`)。**M4** `galley` CLI binary（6 subcommands · NDJSON · 错误 JSON on stdout · exit code 0/1/2/3/4 · `GALLEY_DB_PATH` 覆盖）+ 6 integration tests (`e3f11a1`)。**M5** `docs/agent-api.md` 公开契约 draft（stability promise · exit code 表 · 6 commands field tables · error envelope · B2+ planned commands）(`3e29dbc`)。**M6** GUI `loadSessions` 迁到 Rust core 路径 (`80feb4c`) — JC dogfood OK，建立 B2/B3 用的 10 步 migration template + 4 retrospective notes（Brief vs Full / Option<bool> 语义 / Tauri arg shape / stale-runtime tolerance）。**M7** 11/12 A acceptance 过 + 1 deferred（`--pretty` 推到 B4 polish）+ A11 perf gate 全 < 100ms（10-run avg 60-89ms）。**5 个 O resolved**：O1 workspace 在 core/ · O2 driver = sqlx · O3 error 走 stdout · O4 health 部分实现 · O5 GUI 模板 = loadSessions。**3 新 O**：O7 schemars 时机 · O8 invariants.md §I4 ↔ playbook T1.15 文档矛盾 · runner-manager runtime status 问题。**Rejected**: 3-commit M1 / 仓库顶 virtual workspace / rusqlite / schemars 在 M2 / thiserror / `--pretty` / `help --as-agent` / pnpm tauri build 在本 session。下一步 B2 (runner ownership 迁 Rust)，B1 比 PRD 估的 3 周快 ~21× 完成。 |
| 2026-05-20 | [Disk cleanup + repo hygiene pass](./2026-05-20-disk-cleanup-and-repo-hygiene.md) | 32G → 122M 一次清干净（cargo clean 25.0 GiB + tray-mode spike target 16G + node_modules + .venv + python-bundle + caches）。Repo hygiene 评估出 4 类问题，session 内执行了 A+B+C 三类：A 删 root `node_modules` + `ga_workbench_bridge.egg-info`（**项目旧名残留**）+ 10 处 .DS_Store；B 把 3 个 untracked 大文件（GenericAgent arxiv PDF 3.1M / hello-generic-agent.pdf 16M / galley_icon_v2.png 2.2M）挪到 `~/Documents/galley-refs/`；C `git mv "docs/GenericAgent Workbench-handoff" docs/design-handoff` 去掉**带空格的目录名 + 旧项目名**双重问题（CLAUDE.md:492 + .gitignore uploads/ 路径同步）。**Devlog 不回写历史**：2026-05-13 devlog 当时记录"决定保留 handoff 目录名"，今天反转后写新 entry 而非改旧的——decision provenance 诚实性。**首次 rm 失败教训**：tray-mode/target/.../out/ 目录的 `.DS_Store` 带 `com.apple.FinderInfo` 扩展属性 + Finder 监视 → 第一次 `rm -rf` Directory not empty，B4 M2 重启时记得 close Finder 窗口先。**Rejected**: 删 v1 `docs/galley_icon.png` 孤儿（user 没说要删，保守保留）/ 删整个 design-handoff（CLAUDE.md 还引用作视觉参考）/ 删 tray-mode 源码（WIP commit 还在）/ stage user WIP 的 manager.rs / runtime.ts / sessions.ts（不是本 session 改的）/ 同 commit 起草开源标配文档（user 选 A+B+C 不含 D）。**Open**: D 类开源标配文档（CONTRIBUTING / SECURITY / issue template 等）何时启动 / galley_icon v1 孤儿命运 / README 双语 default 是否反过来（英文 default + .zh 副本）/ 后台 IDE watcher 持续重建 target 和 node_modules 怎么治。**Next**: D 类下次 session。本 commit 不 push（按 CLAUDE.md 规则）。 |
| 2026-05-20 | [Demo / mock cleanup + alive-bridge cap bump](./2026-05-20-demo-mock-cleanup-and-lru-bump.md) | 4 atomic commits 把 V0.1 时代的脚手架一次性清干净。**LRU cap 5 → 20** ([4a742b6](../../commit/4a742b6))：multi-session dogfood 撞 eviction 太频繁，bundled Python ~100MB × 20 ≈ 2 GB 在 8 GB Intel Mac 仍宽裕。不加 setting（surface 税不值）。**Mock audit 三类拆分**：(A) `DEMO_*` 名字下其实有 5 个生产默认值（`DEMO_GA_CONFIG` / `DEMO_APPROVAL_CONFIG` / `DEMO_LLMS` / `DEMO_LLM_DISPLAY_NAME` / `DEMO_RUNTIME_INFO`），改名 `DEFAULT_*` 搬到新 `stores/defaults.ts` ([4fda12a](../../commit/4fda12a)) — 顺手修 `workbenchVersion: "0.1.0"` 撒谎成空字符串 sentinel；(B) 纯 dead code（`DEMO_SESSIONS` / `buildDemoTurns` / `buildDemoPending` + App.tsx `conversationStarted` ternary + `appr_demo1` OR）：触发条件 `activeSessionId == null` 时 MainView 已经走 EmptyState 不渲染 Conversation 路径不可达 ([8fca18e](../../commit/8fca18e))；(C) DevScreenToggle 链路（intro/empty/main toggle + "+toast" + "+mock" + bridge spawn/kill）+ `seedMockSessions` + `MOCK_TITLES_*` + `MOCK_SUMMARIES` + `MOCK_STATUSES` + orphan `shutdownAllBridges`：两个版本 ship 过用真数据 dogfood，脚手架收益边际为零 ([802bfed](../../commit/802bfed))，整个 `stores/demo.ts` 文件删除。**Net**: +89 / -569 LOC 跨 4 commits。**Rejected**: LRU 加 setting / squash 4 commits 成一个 cleanup（atomic 粒度更利 revert）/ 保留 DevScreenToggle 等 visual review / 保留 shutdownAllBridges 等 window-close 接入。**Next**: 不影响 B4 节奏。 |
| 2026-05-20 | [B4 M8 完成 — Pre-migration backup 机制(B4-I6 兑现)](./2026-05-20-b4-m8-migration-backup.md) | B2 M5 已提前把 supervisor/origin 字段 ship 进 mig 006/007,M8 剩余实义 = backup 机制本身:迁移前整目录备份 + 恢复路径,sub-plan 与实现同 session;T8.7 真跑 dogfood 留待下一个含 migration delta 的版本触发。 |
| 2026-05-21 | [v0.2 beta agent surface + historical-session restore dogfood](./2026-05-21-v02-beta-agent-surface-and-history-restore.md) | Settings -> Agent copy simplification, `v0.2.0-beta.1` target, copy-first Supervisor SOP, Core-authoritative architecture reaffirmed, historical-session restore/model switching/continue fixes dogfood pass, and Session Close SOP introduced. |
| 2026-05-21 | [Supervisor provenance + delegation dogfood](./2026-05-21-supervisor-provenance-delegation-dogfood.md) | Supervisor 标识从 top bar 收回到 message-level robot marker；明确 `--supervisor=...` 才算 supervisor provenance；`session new` 从 persisted-only 改为真实 delegation dispatch；修并发 id、GLM 5.1 displayName resolver、SOP discovery first-line；并完成 CLI/Supervisor 边界、GLM/Chrome delegation、错误路径、stop/resume/archive/restore dogfood。 |
| 2026-05-22 | [GA baseline upgrade b063518 -> 1a8abc4](./2026-05-22-ga-baseline-upgrade-b063518-to-1a8abc4.md) | 官方 `lsdefine/GenericAgent` main 升到 `1a8abc4`。36 commits 审计发现 `BaseHandler.dispatch` callback 被 `plugins.hooks` 替代；Galley 加 dispatch feature detection，保持审批 gate 和 live turn_start；`pyproject.toml` 无依赖变更。 |
| 2026-05-22 | [Auto Update Phase 1](./2026-05-22-auto-update-phase-1.md) | Settings -> About 检查 / 下载 / 重启入口落地；更新通道由 `GALLEY_UPDATER_PUBKEY` + `GALLEY_UPDATER_ENDPOINT` 显式启用；release-only 配置生成 signed updater artifacts；手动 promote beta manifest，默认 Dev 不受影响。 |
| 2026-05-22 | [Conversation UX and Update Closeout](./2026-05-22-conversation-ux-and-update-closeout.md) | ManView 等待反馈改成整句 typing indicator；中途 narration 留在主流但降权；assistant 长单行折行；dot rail dense-session 聚合与可点击 hover；updater 加运行中保护 + release/update SOP；Settings/UI primitives 与 project context 收口。 |
| 2026-05-22 | [Project Review sidebar UX](./2026-05-22-project-review-sidebar-ux.md) | Projects 从 inline filter/banner 改为 Quick Action 进入的 Project Review；支持多项目展开、Active/Older 分组、项目对话独立创建、assign toast 反馈、轻量 `+` 样式与模式切换动效。 |
| 2026-05-25 | [Managed GA runtime closeout](./2026-05-25-managed-ga-runtime-closeout.md) | Managed / bundled GA 从方案进入 dogfood：attach/managed 边界、Provider/Model 分离、Galley Persona、packaged app gate、CLI sidecar、lazy Keychain access 全部落地；下次从无启动 Keychain prompt + managed message smoke 继续。 |
| 2026-05-25 | [Settings Models + Runtime UX polish](./2026-05-25-settings-models-runtime-ux.md) | Settings -> Models 按 Provider 重组、常用 Provider presets、模型排序改上下按钮、Provider 检查与模型测试分离；Runtime 和 Sidebar 改为默认内置 GA 安静、外部 GA 轻提示；模型配置导入导出延后。 |
| 2026-05-26 | [Background Mode · Tray Lifecycle](./2026-05-26-background-mode-tray-lifecycle.md) | 默认后台运行：关闭窗口改为隐藏，macOS status item / Windows tray 提供 Open/Hide/Quit，真退出在 Agent 运行中才确认；tray 图标先做极简静态版，Check for Updates 接入 macOS app menu。 |
| 2026-05-26 | [Setup Assistant Re-Entry](./2026-05-26-setup-assistant-reentry.md) | Settings -> Runtime -> More 增加 Open Setup Assistant 深入口，复用首次安装完整向导；入口本身无副作用、不清空历史，运行中禁用，已配置模型可直接继续。 |
| 2026-05-26 | [Managed Credentials · Local Encrypted SQLite](./2026-05-26-managed-credentials-local-sqlite.md) | unsigned beta 阶段放弃 Keychain 默认后端，改用本地加密 SQLite row 保存 managed model API Key；解决 macOS 未签名弹窗，并让备份/迁移保留模型凭据，明确这不是系统级 credential store。 |
| 2026-05-26 | [Session Progress + Model Settings Polish](./2026-05-26-session-progress-and-model-settings-polish.md) | Sidebar 运行/未读状态改为诚实 liveness；主对话等待 3 秒读秒、60 秒才显示仍在运行；Settings -> Models 收敛 Provider row、Onboarding、dropdown、toast 层级和高级配置；session 模型持久化从 index 改为稳定身份。 |
| 2026-05-27 | [GA upstream upgrade 1a8abc4 -> 1c9f141](./2026-05-27-ga-upstream-upgrade-1a8abc4-to-1c9f141.md) | 首次按 external baseline + managed GA rebase 双门跑官方 upstream 升级；补 clean-source guard、`connect_timeout` adapter、`aiohttp` bundle 依赖和 managed payload import smoke。 |
| 2026-05-27 | [Browser Control as managed GA completion item](./2026-05-27-browser-control-managed-ga.md) | Browser Control 作为内置 GA 核心完成项：稳定扩展目录、TopBar 强引导、启动弹一次 setup、确定性 probe、`tabs.create` 百度天气 demo。 |
| 2026-05-27 | [Main surface polish closeout](./2026-05-27-main-surface-polish-closeout.md) | Browser Control 成功态收静、Supervisor 入口放入 managed-mode sidebar header、assistant 流式样式防跳变、diagnostic 文本可选择、`Galley` 命名与 sidebar/topbar 分隔线统一降权。 |
| 2026-05-27 | [Agent SOP dogfood refresh](./2026-05-27-agent-sop-dogfood-refresh.md) | 真实 Supervisor CLI dogfood 覆盖 discovery、inventory、session/project lifecycle、LLM/error paths；修 `session show` origin 投影、`session watch` 重复错误、CLI user message FTS 索引，并同步 SOP / Claude Skill。 |
| 2026-05-27 | [Project Batch Follow](./2026-05-27-project-batch-follow.md) | Project 确认为 Supervisor 批量任务容器；新增 `session follow`、`project brief/show/follow`，保留 `session watch` live-only，并同步 Agent API / Supervisor SOP / Claude Skill。 |
| 2026-05-27 | [Project Follow Until Idle](./2026-05-27-project-follow-until-idle.md) | 旅行规划 batch dogfood 后补强 `project follow --until-idle --final-show`，让 Supervisor 能盯到 Project 内子 session 全部 idle 后自动收束，并用 `followState` 降低初始 idle 快照误读。 |
| 2026-05-27 | [Supervisor User-Facing Copy](./2026-05-27-supervisor-user-facing-copy.md) | SOP 增加面向新手用户的 Galley mode 话术、示例和安全边界；Settings -> Agent 加两句可复制试用 prompt，避免用户必须阅读整份 SOP。 |
| 2026-05-28 | [Settings refactor closeout](./2026-05-28-settings-refactor-closeout.md) | Settings shell、Models、Runtime 做低风险分拆；Models controller 按生命周期抽 hook；Runtime 只拆纯 UI leaf，并明确停止继续为拆而拆。 |
| 2026-05-28 | [Update Channel Verifier](./2026-05-28-update-channel-verifier.md) | 为 Tauri updater beta channel 增加 live verifier；promote workflow 推送后自动检查 `latest.json` 200、版本、三平台 entries、inline signature 和 asset URL；release dry-run 继续暴露 Windows Git Bash path 需转 native path 才能让 bundled Python import managed GA。 |
| 2026-05-28 | [Windows DB path hotfix](./2026-05-28-windows-db-path-hotfix.md) | Windows 首次安装完成模型配置后进不了主界面，根因是 Rust Core 用 `ProjectDirs::data_dir()` 读 `%APPDATA%/app.galley/data/workbench.db`，而 `tauri-plugin-sql` 迁移的是 `%APPDATA%/app.galley/workbench.db`；统一到 Tauri app config dir 并补 docs。 |
| 2026-05-28 | [Alpha release prep and Browser Control dogfood closeout](./2026-05-28-alpha-release-prep.md) | Windows dogfood 后补 managed GA / bridge / Browser Control 体验收口；Browser Control 从强 modal 改为强但非阻塞引导；release target 改为 `v0.2.0-alpha.1`，只做邀请内测手动下载，不默认 promote 更新频道。 |
| 2026-05-29 | [Managed IM Supervisor · WeChat alpha.2 prep](./2026-05-29-managed-im-supervisor-wechat.md) | Settings -> IM 首版落地：微信扫码接入、托管状态目录、紧凑 IM Supervisor prompt、外部 GA 模式隐藏 IM tab，并记录 `v0.2.0-alpha.2` 发版与更新频道决策点。 |
| 2026-05-31 | [Alpha.2 dogfood UX polish](./2026-05-31-alpha2-dogfood-ux-polish.md) | 小屏 Onboarding、外部 GA runtime probe、模型测试成本提示、Tooltip / Toast 降噪、Channels TopBar 入口和 Issue 使用边界收口。 |
| 2026-05-31 | [v0.2.0 stable release](./2026-05-31-v020-stable-release.md) | 第一个正式版发布：`v0.2.0` 非 prerelease GitHub Latest，macOS/Windows assets 完整，release notes 换成用户视角中英双语，并 promote beta update channel 给老版本用户升级。 |
| 2026-06-01 | [v0.2.1 dogfood polish release](./2026-06-01-v021-dogfood-polish-release.md) | post-0.2 polish patch published as stable GitHub Latest and promoted to beta update channel：深色模式、Channels 重启反馈、更新体验、Sidebar 标题、对话图片保存/打开；本次顺手加固 live update verifier，避免 GitHub raw 缓存旧 manifest 时误判 channel promotion。 |
| 2026-06-02 | [GA upstream upgrade 1c9f141 -> 5f46b438](./2026-06-02-ga-upstream-upgrade-1c9f141-to-5f46b438.md) | 官方 `lsdefine/GenericAgent` main 升到 `5f46b438`；external bridge contract 未断，managed patch stack 重放并补长 prompt temp / `/continue` cache state-root 漏点。 |
| 2026-06-02 | [v0.2.3 Browser Control hotfix prep](./2026-06-02-v023-browser-control-hotfix-prep.md) | 新增 `connected_no_tabs` 诊断，把“插件已连接但没有普通网页”从失败里拆出来；教程改为拖整个 `tmwd_cdp_bridge` 文件夹优先，并增加 Chrome / Edge 测试网页步骤。 |
| 2026-06-02 | [v0.2.4 release prep](./2026-06-02-v024-release-prep.md) | 目标定为 `v0.2.4` stable patch：ChatGPT / Codex managed model provider、Browser Control offline recovery、Settings / archived-session polish；GitHub Release 和 update channel 继续分 gate。 |
| 2026-06-03 | [v0.2.5 Codex backend hotfix](./2026-06-03-v025-codex-hotfix.md) | `v0.2.4` 发布后 dogfood 暴露 ChatGPT / Codex backend 参数兼容问题；`v0.2.5` 收窄为 stable hotfix，已发布为 GitHub Latest 并推送到默认更新通道：Responses input list、强制 stream、Codex backend 不发送 `max_output_tokens`，并隐藏不可关闭的 stream 开关。 |
| 2026-06-03 | [Stable release update-channel default](./2026-06-03-stable-release-update-channel-default.md) | release SOP 改为：stable / patch release 的完成标准包含默认更新通道 promotion 和 live manifest verification；只有明确 `manual-download only` 或 `hold updater` 才跳过。 |
| 2026-06-03 | [Windows updater file-lock fix](./2026-06-03-windows-updater-file-lock.md) | Windows 用户手动覆盖安装时卡在 `python\DLLs\_bz2.pyd`，根因是旧 Galley / bundled Python 子进程占用安装目录；应用内更新改为下载验签后先停止 runner / IM supervisor，NSIS 手动安装加 preinstall hook 关闭安装目录内的 Galley / Python。 |
| 2026-06-03 | [v0.2.6 Memory/SOP and UI polish release](./2026-06-03-v026-memory-sop-and-ui-polish-release.md) | `v0.2.6` stable patch 发布为 GitHub Latest 并推送到默认更新通道：补齐内置 GA Memory/SOP seed，Settings Models 三层视觉和快捷入口，对话运行进展降噪，Windows updater file-lock 修复，以及 selection copy / close tooltip 等小 UI 降权。 |
| 2026-06-03 | [哲学气质定位 · philosophical-voice + austerity 文案](./2026-06-03-philosophical-voice-and-austerity-copy.md) | 给 Galley 注入受后期维特根斯坦影响的哲学气质作差异化。新建 `philosophical-voice` spec + MVP 实现：空状态状态绑定题词（Tractatus 7 + 德文常驻副行）、Composer 三寄存器（运行中新增 by-the-way 兼教 `/btw`，沉到纯函数 + 策展数据 + dev 守卫）、删与题词打架的空状态 prompt 建议。新建 [austerity 准则](../copy-austerity-principles.md)（声音规则，与 copy-language-guidelines 分工，调子 B 冷峻，红线"清楚>简洁>气质"），按区域分 7 批逐条 review 重写 `i18n.tsx` 双语文案。既定约定：保留"请"、YOLO 统一"已开启"、`API Key` 字段名、用户文案用「对话」。Task 7（vitest）owner 决定不做。typecheck + lint 全程过。 |
| 2026-06-04 | [v0.2.7 Windows runtime hotfix release](./2026-06-04-v027-windows-runtime-hotfix-release.md) | `v0.2.7` stable patch 已发布为 GitHub Latest 并推送到默认更新通道：修 Windows issue #9 的 managed `code_run` 子进程 stdin 继承卡住问题，更新检查失败时显示手动下载入口和 phase / endpoint / detail 诊断；图片粘贴确认是更大的 Galley-native 多模态链路，拆后续。 |
| 2026-06-04 | [GA upstream upgrade 5f46b438 -> 5d122e20](./2026-06-04-ga-upstream-upgrade-5f46b438-to-5d122e20.md) | 发布 `v0.2.7` 前刷新内置 GA baseline：external surface 未断；managed patch stack 重放并把 Browser Control recovery、ChatGPT / Codex backend 从直接 payload 改为显式 replay patch。 |
| 2026-06-04 | [Bundled Python runtime contract](./2026-06-04-bundled-python-runtime-contract.md) | 明确 release managed GA 必须开箱使用 Galley bundled Python；attach mode release 默认也借用 bundled baseline deps，外部 Python 只作为自定义依赖 escape hatch；新增 bundled managed-GA import smoke gate。 |
| 2026-06-04 | [Galley Goal V1](./2026-06-04-galley-goal-v1.md) | Goal V1 落成 Core-owned headless Hive：SQLite proposal/goal/task/event、CLI controller、conversational confirmation、Composer Goal Send、TopBar 全局入口；后续补上 desktop master session 交付路径，并把运行时间改成持续投入预算，结果回到发起 Goal 的对话；最新调度模型改为模型型 Master Planner：Master 隐藏规划并写 Core task board，worker 是通用接单者，按具体任务懒创建/唤醒；managed/attach 统一 Galley Goal protocol，明确不调用 GA native `/hive` 或写外部 GA state，同时允许 managed GA 用原生 memory/SOP 沉淀长期经验但禁止沉淀 Goal 协议状态。 |
| 2026-06-09 | [GA upstream upgrade 5d122e20 -> ba19018a](./2026-06-09-ga-upstream-upgrade-5d122e20-to-ba19018a.md) | 当前工作 baseline 升到官方 `ba19018a`：external bridge contract 未断；managed payload 纳入 conductor / TUI / scheduler / user-agent 更新，刷新 state-root 与 Codex patch contexts，并扩展 generated payload whitespace normalization。 |
| 2026-06-10 | [TopBar polish + Browser Control onboarding 减负重构](./2026-06-10-topbar-and-browser-control-onboarding-polish.md) | TopBar 按钮/三 popover/主题菜单补触觉 + focus + 可复用 `.galley-pop-in` 入场；浏览器控制 setup 走方向 C：六步压三拍 + Chrome/Edge 只选一次 + 「遇到问题？」折叠 + elevation 对齐 + 第②步「整个文件夹」加重；测试页入口从已连接态裸按钮移进修复抽屉。Rejected：向导式 / 上图示 / dialog 动效 / 已连接态测试页按钮。 |
| 2026-06-11 | [Settings 逐 tab 视觉一致性打磨](./2026-06-11-settings-tab-by-tab-polish.md) | 7 个 Settings tab（Runtime/Approval/Agent/Channels/BrowserControl/Shortcuts/About）+ 跨 tab 统一。确立 hairline 容器为列表基准（新增可复用 `RuntimeAccordionRow`/`RuntimeActionRow`）；触觉诚实（IM 卡 + Shortcuts 去假可点动效/hover）；brand 纪律（展开不消费 brand，IM running 绿/红红绿灯保留）；编号步骤统一圆形 stepper；Agent 复制 SOP 升 primary + prompt 行可发现性 + SOP 文案；About origin 降 elevation + 版本块补对称 section 标题；徽标/透明度 token 收口。Rejected：单开关藏「更多」/ 两列表异风格 / running 改中性 / Shortcuts 纯留白 / 链接列表容器化。 |
| 2026-06-12 | [GA upstream upgrade ba19018a -> 0def7441](./2026-06-12-ga-upstream-upgrade-ba19018a-to-0def7441.md) | 当前工作 baseline 升到官方 `0def7441`：external bridge contract 未断；managed payload 纳入 Project Mode / Responses client_metadata，并明确不接 upstream 隐式 `example.com` opener，继续由 Galley 显式 Browser Control 测试页负责。 |
| 2026-06-12 | [v0.2.8 Goal and workbench polish release](./2026-06-12-v028-goal-and-workbench-polish-release-prep.md) | `v0.2.8` stable patch 已发布为 GitHub Latest 并推送到默认更新通道：Goal V1 和 master-session 交付、主工作台 / Settings / Browser Control polish、GA baseline `0def7441`、CLI/Core 结构拆分；Windows ARM 不塞进本次稳定矩阵，等 workflow / bundled Python / updater manifest / smoke path 补齐后另做。 |
| 2026-06-15 | [Knowledge Sync rules](./2026-06-15-knowledge-sync-rules.md) | 外部 neat-freak skill 的核心原则被裁成 Galley 自己的轻量 Session Close 子流程：按受众同步知识、避免 `AGENTS.md` 膨胀、允许 no-op、不要机械全量扫 docs 或强制作出文件改动。 |
| 2026-06-16 | [Product Design audit and status surfaces](./2026-06-16-product-design-audit-status-surfaces.md) | Product Design audit 产出证据目录，并推动三组状态可扫性收口：TopBar 分状态/工具簇，Sidebar 保持时间线但强化 row 状态和 supervisor 来源，右侧 question rail 只让最新 tail marker 复用 Sidebar running / waiting 图标语法。Rejected：Operational Status panel、Runtime 进 TopBar、Channels 对未配置用户强提醒、Sidebar 状态排序、右 rail 变第二个状态列表。 |
| 2026-06-16 | [Managed IM Supervisor · Feishu Channel](./2026-06-16-managed-im-supervisor-feishu.md) | Settings -> Channels 增加飞书接入：App ID / Secret 本机托管、官方 `fsapp.py` 长连接复用、`lark-oapi` bundle gate、TopBar 继续聚合 Channels 状态；默认组织内所有飞书用户可访问，首版不做 allowlist / 绑定码。 |
| 2026-06-16 | [Galley Native Runtime Checkpoint](./2026-06-16-galley-native-runtime-checkpoint.md) | `galley_native` 路线锁定为 GenericAgent 的 Rust 语义移植 + Galley 产品化内核；官方 GA reference 锁到 `8e9b04e`；确认 V1 目标是可替代 `managed_ga`，Project 可选绑定 native-only workspace，Goal Hive / Morphling / 自我进化 / 分层记忆都属于必须保留的核心语义；后续拆出 [Galley Native RFC 1-7](../galley-native/README.md)：runtime boundary、model/tool loop、native memory、capability packs、workspace/session continuity、Goal Hive/Morphling、parity/default switch。 |
| 2026-06-18 | [Project Workspace and GA upstream upgrade 0def7441 -> 12655687](./2026-06-18-project-workspace-and-ga-upstream-12655687.md) | 官方 GA Workspace / Project Mode 进入 upstream 后，Galley 恢复 Project 文件夹绑定：GUI 选择文件夹即启用，CLI/API 仍用 `workspaceEnabled` 显式 opt-in；不恢复旧 rootPath-as-cwd；managed GA baseline 升到 `12655687` 并把 Workspace state-root 纳入 patch stack。 |
| 2026-06-18 | [GA upstream upgrade 12655687 -> 53b48aea](./2026-06-18-ga-upstream-upgrade-12655687-to-53b48aea.md) | 官方 GA `main` 前进 1 个 `llmcore.py` 日志提交；managed patch stack 原样重放，内置 GA baseline 升到 `53b48aea`，并让 GUI 用 managed manifest commit 作为外部 GA 对比 baseline，修掉旧 fallback 漂移。 |
| 2026-06-18 | [v0.2.9 Project Workspace and GA baseline release](./2026-06-18-v029-project-workspace-and-ga-baseline-release.md) | `v0.2.9` 已发布为 GitHub Latest 并推送到稳定更新通道：Project Workspace 回归、内置 GA baseline 升到 `53b48aea`、Feishu / Channels 与 Core / GUI 稳定性 polish 汇总进正式更新通道。 |
| 2026-06-18 | [Supervisor SOP Lite + Reference Split](./2026-06-18-supervisor-sop-lite-reference-split.md) | Copy-first Supervisor SOP 从 775 行拆成 277 行 Lite prompt + 376 行详细 reference；Settings / IM 仍复制 canonical SOP，但高级命令、Project / Goal 示例和错误恢复移入 reference，保留 `session wait` 超时不等于失败的核心规则。 |
| 2026-06-19 | [v0.2.10 migration recovery hotfix](./2026-06-19-v0210-migration-recovery-hotfix.md) | `v0.2.9` 的 SQLx/Tauri 事务迁移会让 table rebuild 里的 `DROP TABLE sessions` 触发 SQLite 外键级联，导致旧用户历史消息/工具事件丢失；`v0.2.10` 增加 pre-plugin 安全迁移和本地备份恢复，保护未升级与已受影响两类数据库。 |
| 2026-06-20 | [v0.2.11 process lifecycle hotfix](./2026-06-20-v0211-process-lifecycle-hotfix.md) | Mac 发烫 / 多 GA 进程堆积反馈后补进程生命周期防线：`workbench_bridge.py` 加父进程 watchdog，Core 注入 `GALLEY_CORE_PID`，重复启动时唤起旧实例并退出新实例，避免后台 Core / bridge 套娃累积。 |
| 2026-06-22 | [Composer 图片 intake 拆分 + 拖拽落区](./2026-06-22-composer-image-split.md) | Composer 大组件拆分第一刀：图片 intake 抽成 `lib/composer-images.ts`（纯）+ `hooks/useImageAttachments.ts`（有状态），行为保持，1620→1350 行。dogfood 补两个反馈缺口（满 4 张静默→`too-many` toast；拖拽无提示→drop 遮罩），各自独立 commit。关键被否方案：会话区落区——会把 drag 状态逼上 `MainView` 且 `EmptyState` 重复一套，重新缠绕，改为 Composer 自身落区让两处 composer 白拿。原则：按「缠不缠」拆，UX 变更与结构移动分开 commit。 |
| 2026-06-23 | [GA upstream upgrade 53b48aea -> 70792af](./2026-06-23-ga-upstream-upgrade-53b48aea-to-70792af.md) | 官方 GA `main` 前进 9 个提交；external bridge contract 未断，`pyproject.toml` 无依赖变更；managed payload 纳入 upstream desktop bridge/frontend、loop-mode、conductor、reasoning 字段兼容和 180-turn limit，并刷新图片附件 patch 上下文。 |
| 2026-06-23 | [v0.2.12 image intake and GA upgrade release](./2026-06-23-v0212-image-intake-and-ga-upgrade-release.md) | `v0.2.12` stable patch 发布为 GitHub Latest 并推送稳定更新通道：Composer 图片 intake 拆分 + 拖拽落区 / 4 图上限 toast、内置 GA 升级 `70792af`（loop 上限 180、context_win 90000、凭证存入 state root）、Agent / CLI 连通探测修复；release notes 刻意不写 GA desktop frontend（Galley 不调用上游 desktop bridge）。promote verify 因 CDN 时序误报失败、手动复核通过，retry 预算待下次发版前扩。 |
| 2026-06-23 | [Composer paste-fold split + hook-split closeout](./2026-06-23-composer-paste-fold-and-split-closeout.md) | 继图片 intake 拆分后,把长文本粘贴折叠的关注点拆出 Composer,并为大组件拆分轮收口;App.tsx 与 Composer 判定 settled(该判定后于 2026-07-06 被指示重开,见该日 entry)。 |
| 2026-06-24 | [README 默认语言翻转 · 英文设为 default](./2026-06-24-readme-default-language-flip.md) | 求职作品集定位下把 README 双语默认从中文翻成英文：`README_en.md`→`README.md`、原中文→`README.zh-CN.md`、顶部一键互链；理由是开源英文-default 约定 + 裸链接摩擦不对称（国际读者最不容忍）。同轮 README 打磨（中英同步）：删第一屏彩色功能 badge、副标题去 CLI/session 术语 + 加「数据留在本地」信号、Architecture 三 bullet 并入 intro + ASCII 折叠进 `<details>`、删「早期版本截图」备注。Rejected：中英合并单文件（臃肿 / 锚点弱 / 违约定 / 坏工作流）、保持中文 default、截图标 v0.1.0 版本号。 |
| 2026-06-24 | [TopBar 拆双栏 header + 浅色调色板收敛 + 对话框配色统一](./2026-06-24-topbar-split-and-light-palette.md) | 三件视觉层改动：(1) 全宽 TopBar 拆为每栏各自 44px header（`SidebarHeader` + `MainHeader`），根因是 session title 左对齐后暴露「一条全宽 bar」与「下方两栏」结构错配；两栏两色 chrome/app 靠明度分层，OS 窗口控制各归本栏。(2) 浅色中性轴迭代 warm-yellow→warm-neutral→cool-neutral→true-neutral 后收敛到 **true-neutral + whisper-warm**（`#faf9f8` app，R 比 B 高 ~2 counts），暖度来源从底色带黄转到暖墨水 + 杏沙 brand；新增 `--color-chrome #f4f3f1`。(3) Earlier/Archived 对话框通体 `bg-app` + 抬起输入框 `bg-surface`，统一到 Settings 工作台配色；内嵌确认弹窗保留 `bg-elevated`。Rejected：全宽 bar 内按 sidebar 宽度切两段 / 主体深搜索栏浅的配色翻盘 / command palette 跟翻 bg-app / chrome 复用给 inset。 |
| 2026-06-25 | [GitHub Release notes 英文优先](./2026-06-25-release-notes-english-first.md) | GitHub Release notes 双语顺序跟随 README default 翻转：英文在前、中文在后。已发布的 21 个 GitHub Releases（`v0.1.0-alpha.1` 到 `v0.2.15`）全部在 GitHub 端更新正文顺序；不改 tag、资产、draft、prerelease 或 Latest 状态。16 个稳定版（`v0.2.0` 到 `v0.2.15`）的 `What's New` 改成结果优先写法，维护项下沉到 `Under the Hood`；alpha / 内测 release 保留测试说明内容。 |
| 2026-06-29 | [GA upstream upgrade 70792af -> b1e173dc](./2026-06-29-ga-upstream-upgrade-70792af-to-b1e173dc.md) | 官方 GA `main` 前进 11 个提交；external bridge contract 未断，`pyproject.toml` 无依赖变更；managed payload 纳入 `--func` / `--history`、UltraPlan、HTTP app asset、summary fallback 修复和 worldline 移除，并刷新 state-root、Codex backend、图片附件 patch contexts。 |
| 2026-06-29 | [Composer saved prompts](./2026-06-29-composer-saved-prompts.md) | Composer 右侧工具区新增 saved prompts 入口，点击打开提示词库 dialog（dialog-only）：上方可折叠预设模板库 + 下方可排序自定义；LLM picker 回到左侧模型名 + caret，明确不是 Empty State quick prompt 回归，也不改 Agent API / CLI。 |
| 2026-06-29 | [Pointer-first button focus](./2026-06-29-pointer-first-button-focus.md) | 清理 button-like 控件点击后残留的浏览器蓝色 focus outline；共享 Button/IconButton 鼠标点击不落焦点，hover-only actions 不再用 focus/focus-within 显形，输入框和 inline edit 保留焦点反馈。 |
| 2026-07-02 | [全 codebase review + 前三档可靠性修复](./2026-07-02-codebase-review-and-reliability-fixes.md) | 4 个并行 agent 深读四大模块产出 56 条带 ID findings（8 high），追踪清单在 `docs/audits/codebase-review-2026-07-02/`；按档修完前三档：quick wins、数据安全（迁移事务化 + 恢复不复活已删 Goal）、Stop/审批链路端到端可靠（runner deny-all pending + GUI 乐观回滚）；约定每修一个 high 补对应失败路径测试；下一档是 Goal 子系统（CLI-1 事件窗口根因）。 |
| 2026-07-03 | [气质总纲（文库定位）+ 中文微排印 pass](./2026-07-03-temperament-charter-and-cjk-typography.md) | 承接 2026-06-03 哲学气质第二步：诊断「气质不够」的根因是哲学只住在语言层，感官层还是工具默认方言。认领 galley = 校样盘双关，定位「文库不是作者」；新增 `docs/temperament.md`（定位 + 元规则「清晰不深刻」+ 四承重面 + 气质级拒绝清单）与 `docs/typography-principles.md`（三规范第三条腿，红线 = 排印只作用渲染不改写内容）；代码落 `text-autospace: normal`（对齐 Mac/Win 中西文间距，code 豁免）+ prose `hanging-punctuation: allow-end`。Rejected：继续加题词条件（引文边际收益已为负）、justify、pangu.js。Deferred：版权页 colophon + 题词稀缺性评估。 |
| 2026-07-03 | [About 版权页（colophon）+ 题词稀缺性收敛](./2026-07-03-about-colophon-and-epigraph-scarcity.md) | About 按文库本版权页逻辑重组（版式行 + PI §43 题词压轴，零新增表面）；空状态题词收敛为仅 `silent` 渲染（稀缺性判断，quiet/working 绑定作数据保留）。**记录 owner 定位决策：Galley 定位独立产品（基于 GA 二次开发），开发重心转向内置 managed GA**——「立场」块因附属品框架被删；PRD / AGENTS.md Product Shape / attachTrust 文案的同步留待定位坐实后单独 session。 |
| 2026-07-03 | [About「GA 预算」收敛 + onboarding 引擎称谓 pass](./2026-07-03-about-ga-budget-and-onboarding-copy.md) | 诊断套壳感来自频率不是措辞（GA 在 About 出现 4 次）→ 确立「GA 预算」原则（一次有感情 origin + 一次有事实引擎行，写进 copy-language-guidelines）：tagline 去 GA、版本表只剩 Galley、GA commit 并入上游链接行 detail；题词按 owner 反馈挪到页尾收束。新术语规则：managed 语境用户可见文案称引擎为「内核」（en: engine），attach 语境保留 GA 与「不修改」承诺；onboarding / Health Check managed 条目按此扫过（含 L1-L4 行话清理）。 |
| 2026-07-03 | [定位同步：PRD / AGENTS.md / Runtime 页内核化](./2026-07-03-positioning-sync-prd-agents-runtime.md) | 把「独立产品」定位补进宪法层（实践已跑在宪法前面）：PRD §3 增「自有引擎」bullet、runtime-agnostic 降为架构余量非产品身份、定位演化 blockquote 续写第三段；AGENTS.md Product Shape 补 Positioning 段。UI「内置 GA」全库清零 →「内置内核 / bundled engine」（双语 ~26 处，含 stores/bridge 硬编码兜底；存量会话 runtimeLabel 快照如实保留）；Runtime subtitle 去 GA；README 核对后判定达标不改（文档面事实透明不适用 UI 内核改写）。定位同步待办清单四项全部收口。 |
| 2026-07-03 | [README 双层价值主张重构（中文定稿 + 英文同步）](./2026-07-03-readme-two-layer-value-prop.md) | README 定性为「项目主页 + 能力展示面」：诊断旧稿只答「怎么管」不答「能干什么」（引擎能力是壳定位遗留的空白）。Highlights 重组为「单个 agent 能干活（系统级执行 / 真实浏览器 / 自进化技能 / Token 效率）+ 一支团队管得住（原编排 6 卡）」；Token 效率两轮修正（30K→90K 是 Galley 产品决策；卡内 arXiv 引注删除，沉淀「主张在卡片、证据在文末」）；致谢删价值辩护清单换慷慨单句；模型清单对照真实预设（Gemini 删除防过度声称）。发现五张截图全为 v0.1 dev-mock（调试按钮 / 旧 wordmark 入镜），列为最高优先资产任务。 |
| 2026-07-03 | [Supervisor 主动汇报：定位收敛 + 可行性 spike + 设计](./2026-07-03-supervisor-proactive-reporting-design.md) | 定位收敛：CLI agent-first、主战场 = 内置 IM Channels（BYO SOP 降为维护态生态接口）；管理 loop 缺的一环是主动汇报（贴出去的 SOP 结构上做不到）。设计：汇报范围 = supervisor 发起（origin 字段即判据）、只做飞书（微信 iLink 政策风险推迟）、reporter daemon + 模型组稿 synthetic turn、钉 `GALLEY_SUPERVISOR_ID`、wait ≤10min / reporter 接管分工。Rejected：超长 wait（supervisor 失聪 + 可靠性押最脆层）、GUI session opt-in follow（先靠 IM pull）、session 自己发通知（传输能力不漏进 worker）。同日落地，三处实施简化：轮询替代 live watch（DB 即真相，ask_user 推送随之推迟）、去重 = state 文件 + 模型自查 SKIP_REPORT、fsapp 卡片 hook 守卫录为 patch 0013；失败也汇报。 |
| 2026-07-03 | [Supervisor SOP 前沿模型重校准](./2026-07-03-supervisor-sop-frontier-recalibration.md) | 主动汇报后质量瓶颈回到 supervisor 判断，SOP 按前沿模型默认重划确认边界：Goal 确认拆掉 exact-phrase 咒语（契约字段保留、语义降级为现成回复，行为要求 = 用户以自己语言明确指向该提案的肯定回复），风险动作按可逆性分档（stop/archive 明确服务请求时直接执行 + 报告撤销路径；不可逆/外发仍 ask-first），timed_out 话术改条件式消除与 IM entry layer 的双文本矛盾。四份 skill 副本重同步并记录「无机制防漂移」为候选改进（后由 CI drift gate 落地）。 |
| 2026-07-03 | [Goal 单实例化：一次一个 + 重启自动恢复 + 控制器重入锁](./2026-07-03-goal-single-instance.md) | 定位判断「Goal 是重武器，一次一个」；原状态是无上限并发共抢 20-runner LRU 池且用户无感。内聚三件套：DB 部分唯一索引强制单 active Goal（migration 030 + 事务内 pre-SELECT 友好报错）、Core 启动 resume_active_goals（否则死 Goal 永久占位锁死新 Goal）、fs2 controller.lock 防双开。GUI 预防式禁用入口 + tooltip；新增只读 `goal active` 作为 propose 前预检（additive）。范围外：运行中 goal extend、quota 退避、一键停旧开新。 |
| 2026-07-03 | [README 截图双语重拍：实拍偏差与最终口径](./2026-07-03-screenshot-reshoot-bilingual.md) | 按 screenshot playbook 完成 zh/en 双套重拍（种子注入 + glm-5.2 真跑 hero =《哲学研究》豆瓣中译本 / Goodreads 英文版本对比）。四项偏差采纳为正式口径：Goal 专场撤销换项目视图（02-projects）、空状态收编为第四张（04-empty）、「等你回复」未入选（ask_user 不可种）、hero 从奥德赛排片换书版本对比（排片降为时效型备选）。可行性结论沉入 playbook：goal 可纯种、running 可种且重启存活、ask_user 纯内存态不可种。Open：zh 暗色 hero 外观 tooltip 入镜，重拍或接受 owner 定。 |
| 2026-07-04 | [文档系统整备：生命周期 + 单一索引 + 巨型文档拆分 + 链接门禁 + 资产政策](./2026-07-04-docs-system-overhaul.md) | 诊断真病灶 = 活死文档混住 / 索引三处重复 / 三个 1400+ 行巨型文档吃 agent 上下文。P1 建 `docs/archive/`（refactor/、design-handoff/、english-copy-draft 移入；I3/I5/I6/I9/I11 先收编 engineering-workflow），生命周期用**位置编码**（推翻逐文件 status 标头方案）；P2 docs README 任务表定为唯一路由源，AGENTS.md 砍到 10 行高频 + 指针；P3 DESIGN / agent-api / managed-ga-runtime 各拆 7 文件目录 + 承重 stub（逐字校验，agent-api.md 路径被外部 SOP 引用）；P4 新增 `check-docs-links.mjs` 挂 CI（历史文档只查 docs 内链，首跑修活文档 7 条真死链）；P5 资产政策设在入口管未来不追溯（存量压缩/删除只会让 git 历史更大）。Rejected：Diátaxis 顶层分类（索引里分类比文件系统里便宜）、强修 devlog 43 条指向已改名代码目录的历史死链（改写历史 + 永久噪音）。 |
| 2026-07-03 | [题词稀缺性门控翻案：空状态完全恢复](./2026-07-03-epigraph-scarcity-reverted.md) | owner dogfood 翻案：silent-only 门控使题词对真实工作区永久消失、空状态构图偏空。完全恢复每次空状态按条件渲染；诊断当时论证的错位——稀缺性担忧针对「同一句反复」，而三条件绑定 + 驻留冻结本就是反墙纸机制，正确类比是章节题词而非扉页题词。方法论教训：Denk nicht, sondern schau（理论让位于 dogfood）。版权页不动，§43 双住所接受；「启动级稀缺」折中留档备用。 |
| 2026-07-05 | [Topbar 外观控件语法统一](./2026-07-05-topbar-appearance-controls-grammar.md) | 字号控件自绘「A」+ 轨道滑块自创控件双双重建为「图标按钮 → popover → 共享 SegmentedControl」，主题切换同构统一；popover 选后不关闭（来回切档对比是真实用法）。SegmentedControl 选中态全局强化（bg-hover 轨道 + brand-strong medium 文字，surface/elevated 浅色下几乎同白）；删段级 tooltip 根治 autofocus 误弹；外观偏好移除非默认 brand tint（定型偏好≠可行动信息，常驻高亮是安静工作台噪音）；抽 TopBarIconButton 收编 4+ 处重复节奏；宽度箭头 14px 确认为刻意视觉补偿并双处落档。Rejected：反向统一回下拉菜单、字号+宽度合并面板（1 击变 2 击）、选中段整块 brand-soft（多段控件噪音偏高）。 |
| 2026-07-05 | [主对话区审计与三层打磨](./2026-07-05-conversation-area-audit-and-polish.md) | 三 agent 并行审计 7000 行对话组件后分级修复。T0 正确性：IME 组字回车守卫（`lib/ime.ts`，含 WebKit keyCode 229 怪癖）、草稿按 session 驻留（`lib/composer-draft.ts`，write-through + 图片 URL 所有权移交）、denied 工具从 Galley 自有拒绝载荷识别留痕（`lib/tool-outcome.ts`，明确不做 failed 内容嗅探）。T1：禁用控件 tooltip 可达化（aria-disabled 规则化）、块代码/回显接入字号变量、流式高亮保留上一帧、结算 file_patch 复用 PatchView、旁白时序归位、运行态 hint 教 /btw。T2 一致性批量 + T3 三份设计文档回写（§4.5 重写为 pill/block 双层现状）。Deferred：1b 审批卡锁定态（JC 只批 1a）、Phase 2 协议扩展、对话区键盘故事、审批面固定 px。 |
| 2026-07-05 | [Settings 打磨系列 + tailwind-merge 字号陷阱](./2026-07-05-settings-polish-series-and-twmerge-trap.md) | 八 Tab + 两历史 dialog 过同一套权重纪律（眉标不越级 / primary=下一步 / 动作锚定），规则入 §9。工程重点：tailwind-merge 把未知 `text-ui-*` 归为颜色组、被同 cn() 的 text 颜色类静默删除 → 字号从未到达 DOM，「降字号」盲修无效；像素测量反推 16px 定位，extendTailwindMerge 注册 token 全局自愈。Rejected：Runtime 激进 IA、badge 语法强行统一、砍我的模型副标题、保留 BC 底部 action bar、清空归档并入共享确认。 |
| 2026-07-05 | [Managed IM Supervisor · Telegram](./2026-07-05-managed-im-supervisor-telegram.md) | Channels 第三渠道：复用 GA 上游 tgapp.py + patch 0014 装 Galley 集成缝（env 配置 / 状态 hook / owner 绑定），五层链路打通。配对码 owner-binding 复用飞书模式（差异：换 token 不清绑定，Telegram uid 全局）；im_reporter 泛化为 ChannelAdapter（飞书测试零改动），Telegram 发送走 Bot HTTP API 不碰 PTB 事件循环（限制：报告只发文本）；`post_init` = polling bot 的 running 信号 + InvalidToken 立即 error；owner 持久化按平台分发（原实现无条件写飞书配置，三渠道下是真 bug）。Rejected：暴露数字 ID 白名单输入、reporter 调度进 PTB loop、端口锁进托管 main()。 |
| 2026-07-05 | [Sidebar 状态板审计与三层打磨](./2026-07-05-sidebar-status-board-audit-and-polish.md) | 两 agent 审计 + 主线程核实（修正一处结论：completed 仅 CLI/Supervisor 可写，本地结算=idle+未读填充）。T0 信任修复：三信号优先级统一（error 最先）、cancelled 不再谎称已完成、归档运行中会话弹确认（JC 决策；归档不停止运行）、时间桶跨午夜自动重算（useDayStamp）、编辑中禁右键菜单、pointerdown 切换去 500ms 计时窗。T1/T2：挂载不 pop（渲染期 previous-key latch，post-mount flag 方案不可行）、⋯ 触发 28px 居中、菜单与 MainHeader 同语域、拖拽区补齐、120ms 基线、Plus 统一、空装机隐藏归档 footer（有归档数据必须保留）。**产品决策：鼠标优先，键盘可达性明确出范围**，已入规范防审计复报。 |
| 2026-07-06 | [macOS 红绿灯 dev/release 错位与「接受默认灯」收场](./2026-07-06-macos-traffic-light-dev-release-divergence.md) | v0.3.0 发布后发现：dev 里目测调好的 `trafficLightPosition.y=22` 在打包 `.app` 里没生效，灯停默认位且更小。读 tao 源码坐实 config 经 `draw_rect` 钩子实现、dev 应用而打包版不应用；tauri 2.11 无运行时 setter。两条修复路真机验证后都被否：**A** Rust 原生 shim（objc2 重排按钮 + 事件重应用）对齐了但 **resize 抖动严重**（和 OS 布局每帧较劲）；**B** 压两列 header 44→32px 去够默认灯，**比例太矮不舒服**。**最终 C：接受默认灯为角落 OS 家具、不强求对齐（Finder/Notion/Linear 通行做法），代码全回滚到 v0.3.0、v0.3.1 放弃。** Durable 教训：`tauri dev` 不是原生窗口 chrome 的合法验收环境（必须真 `.app` 验收）；小外观瑕疵别用重机制去追。三个廉价测试（切主题/resize 不跳、灯更小但清晰度相同）逐步排除主题复位与 DPI。 |
| 2026-07-06 | [Goal 模式打磨轮：定位入档、Stop 收尾、进度与结果呈现](./2026-07-06-goal-mode-polish-round.md) | JC 三问题（何时用 / 进行中反馈 / Master-Worker 看不懂）审计后归因两根因：后端信息模型丰富但 GUI 全丢弃、PRD 无 Goal 定位。**定位定案入 PRD §6.4：Supervisor 第一受众，桌面第二（水位=基础体验到位）**，据此裁掉专门 Goal 屏幕 / deliverable 面板 / 事件推送。落地六项：migration 031 `messages.goal_id`（goal↔消息精确匹配，旧行回退启发式）；stop 改简短收尾（`GoalFinishMode`，有材料才收尾、超时 120s cap 直接终态化，顺带修复 stopped 从不进 visible list → toast 永不触发的缺口）；结果卡来路行（GoalBrief additive 三计数，改进版数用 deliverable version 作诚实近似）；pill 背景=时间预算进度条（JC 方案，与撤倒计时决策互补）+ popover 补 latestSummary/任务/耗时；master 内实时任务板（JC 二轮升级；**拒绝逐条追加卡**——状态可变 vs 线程追加必然过期）+ worker 上下文条（新 `goal_context_for_session` 反查）；确认框运行位置行（审计发现项目复用本已存在，缺的只是可发现性）。Goal↔Project：自建仅为无上下文 fallback，worker 锚定迁 goal 记未来方向。Rejected：deliverable GUI 面板（双「结果」并存困惑）、隐藏侧栏 worker、goal extend。 |
| 2026-07-06 | [GUI 大组件拆分 · 第二轮 + 项目上下文逃生口](./2026-07-06-gui-large-component-split-round-two.md) | 在 JC 指示下重开 2026-06-23 "App.tsx/Composer settled、不再按尺寸拆" 的结论——不否定其推理（保留内聚核心），把导航性优先级提到尺寸洁癖之上。四组件按同一原则拆：**沿内聚缝拆、状态机/接线枢纽保留**。MainHeader 1335→286（13 子组件进 `layout/header/`，是 6-24 双栏列头之后动内部的新一步）；App.tsx 1758→1136（`useMessageSend`/`useGoalActions`/`useOnboardingFlow` + OnboardingScreen，仍是接线枢纽，弹窗簇留 return——JC 否 40-prop 的 AppOverlays）；SidebarSessionRow 862→560（菜单/归档弹窗抽出，行状态模型留一体）；Composer 1428→937（取 6-23 授权的 GoalConfirmDialog/LLMPill/样式迁出，`ComposerLLMOption` 再导出保 API；**不抽 useGoalArm**，照 6-23 net-negative 结论）。消重：header + sidebar 逐字节相同的 `SessionTitleEditor` 统一到 `layout/`，差异用 `dragRegionOptOut`/`stopRowActivation`/`className` 门控。Bug fix：非项目视图选中项目 session 会隐式绑定项目、New Chat 落项目里且只有 ⌘K 能清（"没有入口"）——把"将创建到 X"被动提示做成带 × 的可撤销 chip（JC 选 A；根因"不可见状态改行为"即选项 B 留待日后）。行为等价、每步 typecheck 卡关、dogfood 通过。 |
| 2026-07-07 | [架构 review 驱动的重构冲刺 + 三次诚实收回](./2026-07-07-architecture-review-refactor-sprint.md) | Explore agent 扫出三层 12 个 deepening 候选,逐个对着真实代码验证再决定。**做**:G1 turn-index single-home（纯模块 + round-trip 测试）/ G3 三连（3b 会话状态读时派生删 fireSessionMirror + `DurableSessionStatus` 收窄 + 白送修幽灵运行中 bug；3c LLM 显示优先级收进 `resolveDisplayedLLM`；3a byId 收进 `useActive*` hook）/ P3 共享 parent-watchdog（两入口净删 185 行）;前置建 skills 脚手架 + 首个 `CONTEXT.md`。**收回**:ADR-0001 不删 turnIndexOffset 兜底（查清是活的）；ADR-0002 不把 session 写路径统一进 `deliver_turn`（5 handler 失败契约互相冲突、且是文档化 Agent API,统一函数=配置对象反模式）;R2 未核实、P1 最险搁置、P2 报告注水（「12+ 处」实为 6 处）。**元教训**:同一份 review 的事实断言在 R1/P2/P3 三处全被证伪——Explore 给线索不给结论,动手前逐行核实,今天这条纪律挡下一次错误重构 + 一次「复制一份改错行为」。 |
| 2026-07-07 | [Managed runtime prompt 打磨：封闭世界作者事实 + 状态块](./2026-07-07-managed-runtime-prompt-polish.md) | 起因真实幻觉事故（`JC Wang`+`wangjc683` 被反推成不存在的中文名「王建成」），诊断出「prompt 给部分事实模型就补全剩余」规律。三决策：作者事实改封闭世界、「神秘」接替简历行（给模型叙事自洽的「不知道」理由）；术语级规则只收产品名一条（Galley 不全大写）；新增会话启动状态块 `compose_runtime_prompt(version)`（版本/平台/engine，Core 拼装、seam 与 runner 零改动），把「让用户查 Settings」的推诿改成有据可答。字段准入四规则（会被问/spawn 时可靠/会话内不变/可外发），据此排除模型名、渠道等易过期字段；`prompt_hash` 只算静态部分。prompt-composition.md 重写：静态条款三准入 + 条款出处 ledger + 七问 dogfood 回归清单。体量基线 ~900 token。Rejected：注入模型名/渠道、完整 copy-language 规则、runner 侧多变量拼接。 |
| 2026-07-09 | [Goal solo 打磨轮二：过程可见 · 无项目 solo · 收口竞态 · Composer 入口](./2026-07-09-goal-solo-dogfood-round-two.md) | 三问题 + 两轮 dogfood 的六个决策。**方案 A 取代深研形态**（nudge 落库与 turn 派发可见性是独立开关，"刷屏"元凶是 nudge 不是 turn；折叠组记为 v2 退路）/ **无项目 solo 正经迁移**（033 重建 + preflight 连续扩到 33，spec 必须 include_str! 全文；GUI 过滤 hack 被否）/ **收口竞态**（turn_index vs turn_count 错位 + `>=`，与 wait_solo_turn 同类漏改；超时兜底 = best-effort completed + shutdown runner，Wrapping/resume 死路被否；超时与预算挂钩）/ **Composer 两原则**：GUI 门控语义对齐 DB 锁（结束未读 ≠ 占坑）、控件几何稳定 > 视觉强调（armed 宽胶囊把切换钮顶走 84px，砍掉改原位圆钮变形 + ×/◎ 图标区分；空状态启动改"落地后翻屏"）/ **controller.rs 2356 行拆 4 模块**（理由是引擎混编致同类等待逻辑漏改，不是行数；#[cfg(test)] re-export 门面保 tests/mod 零改动）。 |
| 2026-07-10 | [GA upstream upgrade b1e173dc -> 502be0a](./2026-07-10-ga-upstream-upgrade-b1e173dc-to-502be0a.md) | 官方 GA `main` 前进 10 个提交；external bridge contract 未断，`pyproject.toml` 无依赖变更。llmcore 纳入 Claude refusal 不重试、`ChunkedEncodingError` 重试、抑制重试中间态 error yield、opt-in `extra_sys_prompt` / `omit_thinking`（默认关，`backend.history` 形状不变）；agentmain 加 additive `--no-user-tools`。两 patch rebase：0001 的 `ga_ultraplan.py` 缩成一行、**改用上游新 `GA_ULTRAPLAN_RUNDIR` env 缝**并回退 `GALLEY_GA_STATE_ROOT`；0007 Codex helper 块因 `llmcore.py` 行号漂移用 `git diff -U0` 重生（顺带修掉插入 hunk 里潜伏的 152/151 行数 off-by-one，之前被 git 模糊匹配掩盖）。173 pytest / mypy / ruff / 四道 drift-doc 门全绿，GUI fallback baseline 同步。 |
| 2026-07-11 | [Typed socket protocol module + CLI SocketClient](./2026-07-11-typed-socket-protocol-module.md) | 架构评审候选 1:`core/src/protocol/` 成为 schemaVersion 1 的唯一类型之家(信封 + 17 命令 args + SocketCommand 名字绑定 + ErrorTag),CLI 改走字符串行级 Transport 缝上的 SocketClient;金样先行钉住响应字节,字段名漂移从静默丢参变为编译错误。 |
| 2026-07-11 | [Socket write handlers:注入缝(HandlerCtx / RunnerPort / Notifier)](./2026-07-11-socket-write-handler-seams.md) | 候选 2:写处理器依赖注入,`dispatch_line_with` 成为测试入口;10 个新集成测试首次覆盖 session.new 三条 spawn_failed 回滚叙事与事件时序;与候选 1 合计,CLI→Core 写路径两端都有假体,可离线端到端回归。 |
| 2026-07-11 | [AgentTurn 构建单一之家(lib/agent-turn.ts)](./2026-07-11-agent-turn-single-home.md) | 候选 4:live/persist/restore 三处重算收敛为一个模块,persist 直接吃构建产物;往返恒等测试钉住 live===restored;顺手统一 null 预览分叉、修正 persist 存 "" 的现行行为(restore 侧归一化原是承重逻辑而非历史修复)。 |
| 2026-07-11 | [GaSession:GA 集成缝成为模块](./2026-07-11-ga-session-seam.md) | 候选 3(最后一个 Strong):`runner/ga_session.py` 收拢约 10 处 agent 内部 reach-in,范围规则=「重审义务面」;set_history 对未验证后端发响亮警告;基线升级审计从全文 grep 变为读一个文件,ga-baseline.md 的 SOP 已同步指向新入口。 |
| 2026-07-11 | [文档全面体检:陈旧修正 + 防漂移机制](./2026-07-11-docs-governance-round.md) | 三路审计(结构/陈旧/交叉引用)+ 主 agent 核实后定调中档:15 处陈旧修正(schemaVersion 措辞×6、判别符单一来源、行号引用降级为符号)、docs/README.md 补全为真正唯一索引 + 新增 Update Triggers 表(漂移的机制性根因是触发器缺失)、devlog 索引补齐 8 篇、issue tracker 补终态与发货后处置规则;删除违规 rejected 截图与已发货的 goal-solo-hive 工单(残留开放项记录在案)。 |
| 2026-07-15 | [顶栏更新提醒 + toast 严重级驻留 + OAI 恢复验证](./2026-07-15-topbar-update-indicator-and-toast-severity.md) | 更新状态此前只在 Settings 可见。新增 UpdateIndicator 徽章于 StatusCluster 末位（两 cluster 的书面契约决定归属；末位不动常驻按钮肌肉记忆、隔分隔线邻齿轮），显示 available/downloading/ready 三态（available 必须显示：有任务时自动下载会延后）、error 不上顶栏；popover 内显式"重启并更新"（拒绝顶栏一键直接重启——会拆 IM supervisor + runner）。v1 不显示更新说明：通道 manifest `notes` 默认是裸 Release URL。**warning/error toast 不再自动消失**（无 toast 历史 = 自动消失的报错是用户永远拿不回的信息；per-toast `autoDismissMs` 仍可显式短暂；inbox 方案搁置观察）。由此驻留的启动警告牵出 **OAI 恢复误报**：`NativeOAISession` 继承 `NativeClaudeSession.ask()`、内存 history 形状同构，审计后加入恢复验证集（记为只读耦合点；MixinSession 等仍响亮告警）。Rejected：齿轮小蓝点作主入口、UtilityCluster 落位、v1 下载进度条、未审计先静音。 |
| 2026-07-15 | [GA upstream upgrade 502be0a -> 1e89c3e](./2026-07-15-ga-upstream-upgrade-502be0a-to-1e89c3e.md) | 官方 GA `main` 前进 362 提交，但几乎全是社区 GA Desktop 前端合并（PR #671）；引擎核心 delta 仅 12 文件 ~380 行，`agent_loop.py` / `pyproject.toml` 零变更。纳入 file_write/file_patch 换行符保持、`_arg` 类型强转、工具输出上限随 `context_win` 缩放、`trim_keep_prefix`、OpenAI `reasoning_effort: max`、TMWebDriver safe_print、MixinSession routed-facade 重构（history 仍是普通 get/set，保持在恢复验证集之外；新 `__setattr__` 对 node 级属性抛错——managed 配置不产 mixin，`extra_sys_prompt` 写入安全）。**0005 整个删除**（上游 TUI-v2 撞上同一 stdin 继承 bug，独立写出逐字节一致的修复——殊途同归而非引用 Galley）、**`_ga_project_mode_name` 属性缝升级为 Project Mode 唯一机制**（该缝系上游 TUI 六月先引入、Galley 6-18 跟进采用；pid 文件锚删除，0001 的 project_mode hunk 缩成 state-root 重定向一行）。patch 栈整体改用**提交链 rebase 法**重生成：首次逐个重放时 0002/0006 零上下文 hunk 在位移文件上「成功」落错位置（py_compile 才暴露），旧链先逐字节复刻已发布 payload 再 rebase，真冲突只有两处手工解。`verify_sop → deliverable_audit_sop` 改名传导到 payload 检查脚本关键文件清单。188 pytest / mypy / ruff / payload 检查 / py_compile 全量扫描 / mac-x64 bundle smoke 全绿；GUI fallback baseline 同步。 |
| 2026-07-15 | [桌面工艺打磨轮：窗口记忆 + 菜单栏精修 + skeleton 否决](./2026-07-15-desktop-craft-polish-round.md) | 起点是 JC「手感不如大厂扎实但说不上来在哪」，十维实证审计发现静态工艺层已高于独立产品平均，缺口在别处，本轮全修：**窗口状态持久化**（tauri-plugin-window-state，排除 VISIBLE 防后台隐藏态被存成启动态、排除 DECORATIONS 防覆盖 Win 自定义 chrome）；**删两个永久灰色菜单占位**（Toggle Sidebar 被「sidebar 不可折叠」产品决策否决；Find 的一半职责已被 Cmd+K FTS 接管，另一半 find-in-page 无排期——菜单项不是 backlog，从 v0.2 灰到 v0.3.2 读作「坏了」）；**菜单栏补系统标准项**（Services / Enter Full Screen / NSApp Window+Help 注册——Help 搜索框的 AppKit 标题启发式在中文系统静默失效，必须显式注册）；**Conversation Width 换 CheckMenuItem** + `set_width_menu_state` 向内镜像（照 set_close_hint_copy 模式）+ Rust 点击处理器即时翻勾防瞬时双勾。**加载议题重表述**：不是「缺 skeleton」而是三个布局不稳定源——原子会话切换（先恢复后翻指针 + epoch/指针双守卫；单测抓到 `!== null` 对 undefined 恒真的守卫失效 bug）、Shiki 度量恒等（!important 中和 bold/italic token，高亮退化为纯着色零回流）+ 300 条 LRU（重挂载首帧即彩色）、图片 natural 尺寸缓存预留盒子。**Skeleton 明确否决勿翻案**（本地读几十毫秒 + 转录结构不可预测 + shimmer 属 B 类环境动效）。Open：菜单本地化、Cmd+K 菜单入口（均待 JC 裁决）、会话内搜索排期、Win 裸滚动条、延迟方差无基线。 |
| 2026-07-16 | [更新下载真实进度条 + spinner 统一](./2026-07-16-update-download-progress-and-spinner-unification.md) | 兑现 07-15 顶栏更新轮显式推迟的下载进度：Rust `install_app_update` 内接通 Tauri 下载回调，广播 `app-update-progress` 事件（沿用 im_supervisor 的 emit 模式，拒绝无先例的 `ipc::Channel`）；payload 分 downloading/installing 两相——下载完还有停子进程 + install 数秒工作，UI 不能冻在 100%。Rust 侧 `ProgressThrottle` 节流（首 chunk / 整数百分比变化 / ≥150ms，时间作参数可单测）。store 先 listen 后 invoke（bridge.ts 同款时序）、`finally` unlisten、迟到事件不得复活 ready/error 终态；`downloading` 状态 additive 扩展。UI：popover 内 3px `bg-brand/15` 轨道 + `bg-brand` 填充（Goal pill 同款成语）+ `role="progressbar"` + `tabular-nums` 百分比；24px Settings chip 只加 `· NN%` 后缀不塞第二条 bar；`total: null` 回退 spinner、百分比 clamp 防 Content-Length 说谎。不违反「不做假进度」——那条否决的是 app 不可知的量，下载字节是真实的。顺手统一 spinner：10 处 `animate-spin` → 户内 `.spin`（动画集中 globals.css 纪律；1s→1.4s 节奏）；reduced-motion 下 `.spin` 放慢到 4s 而非冻结（静止 CircleNotch 是歧义字形），bar 填充过渡则完全关闭。Rejected：徽章内 ambient 填充（popover-only 保持徽章安静）。 |
| 2026-07-16 | [Windows 滚动条打磨（Mac 不动）](./2026-07-16-windows-scrollbar-polish.md) | 关闭 07-15 审计的「Windows 裸滚动条」open item。全仓库原本零自定义滚动条样式——macOS overlay 本就是理想形态，问题只在 Windows 经典灰条撞暖纸色调。方案：`lib/platform.ts` 新增 `applyPlatformAttribute()` 启动时设 `html[data-platform]`（仿 theme.ts 的 dataset 模式；不需要 index.html 内联脚本——滚动条晚一帧不可见），全部规则以 `data-platform="windows"` 限定，Mac 路径 byte-identical（platform.ts 既有守则）。选 `::-webkit-scrollbar` 伪元素而非标准属性，两条引擎事实定案：WKWebView 命中任何该伪元素规则即失去 overlay 自动隐藏（故必须平台限定）；Chromium 121+ 标准 `scrollbar-width/color` 会静默禁用同元素 webkit 规则且表达不了 hover/圆角/内缩——双禁令写进 globals.css load-bearing 注释 + foundations §2.6。样式：10px 双轴、透明轨道、胶囊 thumb 内缩 2px（视觉 ~6px）、`line-strong`→hover `ink-muted`→active `ink-soft` 随 `data-theme` 自动翻转、透明 corner 消白角块；通用后代选择器免逐组件处理。三大面板加 `.scrollbar-stable`（scrollbar-gutter: stable）杀溢出阈值横移，代价是 Windows 转录居中列恒偏 ~5px（不可感知，备选 `both-edges`）。Rejected：动 macOS 滚动条、JS 滚动库 / Radix ScrollArea、plugin-os 检测。Windows 实测挂 smoke checklist 新增 Scrollbars 节。 |

## 格式约定

每个 entry 6 段：

- **Date / Status / Related** — 元信息（含 PRD/DESIGN/commit 引用）
- **Context** — 这次讨论或工作的背景
- **Decisions** — 对齐的具体结论，列表化、可索引
- **Rejected alternatives** — 考虑过但没选的方案 + 理由（最有价值的部分）
- **Open questions** — 留待后续的问题
- **Next** — 这次工作的下一步

## 触发时机

主动写 devlog 的三种场合：

1. 每次 work session 结束（"今天先到这里"）
2. 重大设计/架构决策对齐后（不一定等 session 结束）
3. 阶段切换（如 Stage 1 → Stage 2，写一份阶段总结）

## 写作责任

- Claude 主写：每次决策对齐后主动提议落 devlog
- 作者 review：可以 inline 调整，Claude 根据反馈改
- 不重复信息：devlog 不复述 PRD / DESIGN.md / CLAUDE.md 已有的内容，只记叙事 + decision provenance

## 文件命名

`YYYY-MM-DD-topic-in-kebab-case.md`，一天可以多个 entry（按主题分）。
