# v0.4.10 发布：搁置批次兑现（baseline 30b24ad + Goal 派发修复领衔）

**日期**：2026-08-24
**范围**：v0.4.10 发布叙事，聚合 `v0.4.9..HEAD`（9 个提交，实质改动 5 个：
GA baseline bump、dark 画布抬升 + chrome 翻向、sidebar 选中行三通道、
Goal 派发装门、步骤密度 pass）

## 背景：这是 8-21 被搁置批次的兑现，外加两条新增

8-21 JC 裁决「攒一攒再发」时，批次为 baseline bump + 两项 dark/sidebar
视觉改动，已定级 patch（`v0.4.10`）、pre-flight 已过绿。搁置后又攒进：

- **Goal 派发装门 + 真实忙闲信号**（`d3408fa0`，Rust Core + CLI）——
  社区上报的 solo Goal bug 对（galley#19 toast 刷屏 + 停止后假死）。
- **步骤密度 pass**（`b33093b6`，GUI-only）。

触发发版的是 Goal 修复：用户上报的修复压在手里不发，正是 v0.4.8 /
v0.4.9 两次用过的 ship-now 理由——未发运的修复加厚下一版未验证 delta，
发布本身就是验证通道。

## 搁置记录点名的两项复查（不继承，重查）

1. **上游 GA 位置**：上游 `main` 已从 `30b24ad` 前进 10 个提交到
   `9e68c20`。逐文件 compare 审计结论：**引擎核心零变动**（`ga.py` /
   `llmcore.py` / `agent_loop.py` / `agentmain.py` / 根 `pyproject.toml`
   全部不在 diff 中），全部增量是上游自建 Desktop 2.0 前端（编译 dist
   资产、字体、打包脚本）+ conductor / cost_tracker / data_backup 等
   辅助前端 + 上游自己的 CI 与测试。这些文件虽在 vendored 全树范围内，
   但不在 Galley 执行路径上，bump 只会给 bundle 塞惰性资产并作废已绿的
   打包门禁。**裁决：本版按已审计的 `30b24ad` 发，不 bump**；下个版本
   发版前照例再审。
2. **`context_management` dogfood**：JC 已在 native Claude 长会话实测，
   无异常（2026-08-24 确认）。

## 版本决策：patch，免议

8-21 搁置记录已终裁 patch 且写明「不需要重新论证」：与 v0.4.8 同构——
baseline bump 当头条、GUI 打磨随行；Goal 修复是 bug fix，密度 pass 是
打磨，均不改变定级。

## 门禁

| 门禁 | 结果 |
|---|---|
| `check.yml`（main @ `b33093b6`） | 绿 |
| `pnpm typecheck` / `lint`（密度 pass 落地时，同一树） | 绿 |
| `cargo check --workspace` / `cargo test --workspace` | 绿（129 tests） |
| bundled runtime gate（bundle-python + managed-ga 校验） | 绿（8-21 mac-x64；此后 `managed-ga/` `runner/` `scripts/` 零提交，结果可继承） |
| `check-version-consistency.mjs --tag=v0.4.10` | 5 文件一致 |
| runner 门禁（pytest / mypy / ruff） | 未跑——本批次 `runner/` 零变动 |

## 范围来源

按惯例范围只认 `git log v0.4.9..HEAD`，且对 52 文件全量 diff 交叉核对
过 project-status 散文（本次吻合，无漏项）。What's New 覆盖四项实质
改动；纯文档提交（两条 deferred 记录、texture 实验记录、v0.4.9 状态
同步）不进 release notes。

## 发布执行

- 更新渠道策略：stable patch，publish + smoke 后照常 promote。
- Draft 关口照 SOP：agent 停在 draft，JC 真机烟测（含 Windows x64——
  本批含 Rust Core/CLI 改动）后明示批准再 publish + promote。
