# v0.4.2 发布:定时任务信任面 + 引擎升级到 d8d90ee

**日期**:2026-08-03
**范围**:v0.4.2 发布叙事,聚合 `v0.4.1..HEAD`(11 个提交)

## 背景

`v0.4.1` 之后累积了三条线:定时任务的信任面打磨(issues 11–14)、
Question Rail tooltip 的回答预览,以及 GA baseline 从 `4086d5c` 升到
`d8d90ee` 并附带一个新补丁 `0016`。三条线各有独立 provenance,本条只记
**为什么打包成 `v0.4.2`** 以及发布点的聚合状态。

## 版本决策:patch

沿用 `v0.4.1` 确立的定级规则(按功能量级判,minor 要求够一条独立产品线):

- 定时任务信任面是 `v0.4.0` 那条产品线上的增量打磨,不是新面;
- rail tooltip 是辨识度打磨;
- 引擎 baseline 升级不改产品形态。`v0.2.16` 发运审计后 baseline 时同样
  走 patch,先例一致。

产品形态、Agent API schema(`schemaVersion: 1`)、更新渠道策略均不变。

## 聚合的线

| 线 | 状态 | provenance |
|---|---|---|
| 定时任务信任面(失败可见性 / 表单触发预览 / 立即运行 / 自启引导) | issues 11–14 全 done | [信任面打磨轮](./2026-07-30-scheduled-tasks-trust-polish.md) |
| Question Rail tooltip 回答预览 | 完成 | [rail 回答预览](./2026-08-03-question-rail-answer-preview.md) |
| GA baseline `4086d5c` → `d8d90ee` + 补丁 `0016` | 完成,本次首发 | [GA 升级](./2026-08-03-ga-upstream-upgrade-4086d5c-to-d8d90ee.md) |
| TurnMarker 副标题与回答重复的渲染层去重 | 完成,draft dogfood 后追加 | [summary 去重](./2026-08-03-turn-summary-echo-dedup.md) |

### 引擎升级是本次的重量项

上游 20 commits 里对 Galley 有实质影响的是**中断响应性与流式健壮性**:
停止现在会真正撕掉在途 HTTP 流(`active_response` + `should_stop`),
而不是让它跑完;`retry-after` 有了 `max_retry_after` 封顶(默认 60s),
超大值不再吊死会话;Responses API 的 `response.incomplete` / `response.failed`
被当作合法终止事件,不再触发空响应重试风暴。

同轮引入的 `0016-managed-native-thinking-tags.patch` 是**发运前提**而非附带:
上游开始把 Anthropic 原生 thinking 无标签地 yield 进输出流,而 Galley
剥的 `<thinking>` 是提示词约定的另一条通道。因为 `managed_model.rs` 对托管
Anthropic 模型默认下发 `thinking_type: adaptive`,这是**默认命中**路径——
不打补丁直接发版,用户会在回答正文最前面看到推理内容,思考面板反而空,
且脏数据会永久落进 `final_answer` 与 FTS 索引。补丁把新通道归一到既有标签
约定,顺带让原生推理第一次正确进入思考面板。

### 不随本次发运的东西

- `.scratch/scheduled-tasks/issues/06`(CLI schedule 命令面)仍是
  `needs-triage`,延续 `v0.4.0` 的有意 v1 非目标:GUI 已闭环,等真实
  supervisor 用例出现再动。它属于 Agent API 面,风险等级与 GUI 打磨不同,
  不该被一个 patch 版顺带捎上。
- 遗留开口:`extractThinking` 只取**第一个** `<thinking>` 匹配,当模型同时
  产出原生推理与提示词叙述时,后者被剥掉而不显示。两者都正确排除在回答之外,
  属展示取舍,留给 JC 定。

## 已知限制(自 `v0.3.7` 携带)

Windows 上 Alt+Tab 切回仍需先点一下才能输入,搁置在 tauri 2.12 触发线后
(`.scratch/win-composer-focus/`,devlog 2026-07-21-windows-composer-refocus)。

## draft 重打:装机 dogfood 发现同一段话显示两遍

第一次 tag(构建绿、draft 资产齐全)后,JC 在装机 dogfood 中发现 Anthropic
协议模型的无工具回答轮会把回答重复显示。排查结论是**既有缺陷、非本次回归**
(详见 [summary 去重](./2026-08-03-turn-summary-echo-dedup.md)),
按发版规则本可推到下一个 patch。**JC 裁决卡住 draft、修完重打 tag**——
理由是这一版本来就是引擎升级版,带着一个"看起来像升级引入"的显眼展示缺陷
发出去,用户与我们自己都会把它归因到升级上。

操作按 SOP 失败路径:删远端 / 本地 tag + 删 draft Release,补修复提交后在
新 HEAD 重打同一个 `v0.4.2`(版本号未发布过,无需再 bump),重新走构建。

## 发布 gate 状态(打 tag 前本地验证)

全绿:

```
check-version-consistency --tag=v0.4.2   5 files agree on 0.4.2
cargo check --workspace                  OK
cargo test --workspace                   365 passed
pnpm --dir gui typecheck / lint          clean
pnpm --dir gui test                      260 passed (36 files)
pytest runner/tests -m 'not e2e'         193 passed
mypy runner                              no issues, 22 files
ruff check runner                        passed
git diff --check                         clean
bundle-python.sh mac-x64                 OK (bundle 153M)
check-bundled-python-managed-ga.sh       OK (bridge-ready)
check-managed-ga-payload.mjs             OK
check-ga-baseline-drift.mjs              OK (d8d90eef)
check.yml on HEAD                        green
```

e2e 未跑(需真 GA + LLM 与模型额度)。构建机是 Intel Mac,故 bundled runtime
gate 走 `mac-x64`;`mac-arm64` 在本机会以 `Bad CPU type in executable` 失败,
这是预期的交叉架构限制,arm64 产物由 CI 出。

## Next

标准 release SOP:tag/push → `release.yml` 出 draft → JC 装机 smoke
(macOS + Win11;本次重点感受引擎升级后的**停止行为**——按停止应立即断流,
以及托管 Anthropic 模型的思考面板是否正确收纳原生推理)→ publish →
promote stable channel → 验证 live manifest → 回填 project-status。
