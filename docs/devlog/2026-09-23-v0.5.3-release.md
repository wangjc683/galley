# v0.5.3 发布

**日期**：2026-09-23
**上下文**：v0.5.2 后一天，main 上积了思考实时预览（带内核补丁）和四项对话区
打磨。JC：「是不是可以发版了」→「发 v0.5.3」。

## 裁决

- **发，patch。** 按最大单个功能的量级分：思考实时预览是 IPC 增量字段
  （`turn_end.responseThinking`），无迁移、无 schemaVersion 变化、CLI 未动。
- **打包门禁必须跑**：`managed-ga/`（补丁 `0016` 重写，`0017` / `0021` 漂行号）
  与 `runner/` 都动了。
- app-update hop `v0.5.1 → v0.5.2`（v0.5.2 的 SOP step 10 欠账）：JC 报告已装机
  验过，没问题，核销。

## 范围（git log v0.5.2..HEAD，7 提交 + 本次 bump）

- 思考实时三行预览 + 推理落定进 caret；`0016` 改为带内边到边吐标签，两通道同构
  （`81b8cc75`，[devlog](./2026-09-23-live-thinking-preview.md)）
- 旁白回声步：旁白当步骤标题（`e5d6e719`，[devlog](./2026-09-23-step-heading-narration-echo.md)）
- 可展开步骤行的 hover 应答与键盘可达（`d911944e`，[devlog](./2026-09-23-step-marker-hover-affordance.md)）
- 中文加粗不再漏星号，`remark-cjk-friendly` 取代自写插件（`deb471c0`，[devlog](./2026-09-23-cjk-friendly-emphasis.md)）
- 折叠头行内分层（`4dcd0024`，[devlog](./2026-09-23-run-fold-header-hierarchy.md)）
- 不写 notes：对话结构示意图重画（`e3f43955`）、v0.5.2 状态同步（`72590251`）

## Pre-flight（本机实测）

- 本地：gui typecheck / lint / 525 tests；`cargo check` + `cargo test --workspace`
  515 passed；runner pytest 296 passed、mypy、ruff 全绿；check.yml 六个门禁脚本
  全绿。
- 打包门禁 **mac-x64**（JC 的 Mac 是 Intel）：`bundle-python.sh mac-x64` +
  `check-bundled-python-managed-ga.sh` 通过，managed GA import OK，bundle 162M。
- 版本一致性：5 文件对齐 0.5.3，匹配 `v0.5.3`。
- 签名 / 更新器 secrets 与 variables 齐（`TAURI_SIGNING_PRIVATE_KEY(_PASSWORD)`、
  `GALLEY_UPDATER_PUBKEY`、`GALLEY_UPDATER_ENDPOINT`）。
- 更新通道策略：stable，publish + smoke 后同 session promote。

## 冒烟重点

思考预览风险最高（引擎补丁 + 流式路径）：内置 OpenAI 兼容通道与 Anthropic 通道
各一轮看三行预览与 caret 找回；中途 Stop（未闭合标签边角）；`/btw` 旁问不漏
推理；外置模式一轮确认不劣于 v0.5.2。GUI：折叠头在 live run / 深色 / 大字号档、
揭阳回答无星号、步骤行 hover。Windows 装机一轮。
