# v0.4.15 发布

**日期**：2026-09-14
**上下文**：v0.4.14 当天，一条社区 524 报错的对表牵出 `max_retry_after` 的
开放裁决。JC 真机验完后决定单独发一版，让撞到同类中转站的用户有出口。

## 范围（git log v0.4.14..，1 个功能提交）

1. **高级配置开放 `max_retry_after` + 补丁 `0021`**（`43bdf8f1`）——模型
   高级配置新增「重试等待上限」，未设置不写键；内核报错文案带上服务商要求的
   实际 Retry-After 秒数。见
   [高级配置开放 max_retry_after](./2026-09-14-max-retry-after-advanced-option.md)。

另有两个非发布提交：v0.4.14 的状态同步、option-desc PRD 取消（纯文档 /
`.scratch`）。Core 零提交，schemaVersion 不动；`runner/` 只加了测试；
`managed-ga/` 动了（补丁 `0021` + 重生成的 `llmcore.py`）。

## 定级

`v0.4.15`，patch。一个策展字段加一处报错文案，没有新功能面。

## 门禁

- **打包门禁 mandatory**：`managed-ga/` 动了。本机第一次误用 `mac-arm64`
  撞 Bad CPU type（本机是 Intel），改 `mac-x64` 重跑：`bundle-python.sh` +
  `check-bundled-python-managed-ga.sh` 通过。
- 本地：runner pytest 240 / mypy / ruff；gui typecheck / lint；
  `git diff --check`；`check-managed-ga-payload.mjs` OK。Rust 零改动，cargo
  交给 `check.yml` 的三平台 Core job。
- `check.yml`：`43bdf8f1` tag 前等绿。
- 版本一致性：`check-version-consistency.mjs --tag=v0.4.15`。

## Smoke 计划（draft 门）

JC 装 draft 包：编辑模型 → 高级配置，四个数字字段两列两行，「重试等待上限」
在读取超时右边；填 120 保存后 `managed-models.json` 出现该键，填回 60 键
消失。报错文案的实际值没有稳定的复现手段（要中转站真的发 Retry-After），
靠 runner 测试锁住。app-update hop `v0.4.14 → v0.4.15` 发布后走 step 10。
