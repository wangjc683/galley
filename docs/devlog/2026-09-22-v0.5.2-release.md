# v0.5.2 发布

**日期**：2026-09-22
**上下文**：v0.5.1 后四天，main 上积了会话级推理强度 pill、模型高级配置分层、
磁盘占用修复。JC：「可以发版了，你觉得呢」。

## 裁决

- **发，patch。** 我先前说「带迁移走 minor」是错的：project-status 的规则按最大
  单个功能的量级分，027 / 029 迁移都随 patch 发过。pill 与分层都是设置与对话框
  的增量，JC 裁 v0.5.2。
- **打包门禁不需要**：`runner/`、`managed-ga/`、GA baseline 均未动。

## 范围（git log v0.5.1..HEAD，8 提交）

- 磁盘占用：备份跳过引擎 scratch、`model_responses` 保留期（`9b6cb023`）
- Composer 会话级推理强度 pill（`12ba4cbb`）
- 模型高级配置分层 + `max` + 全小写 + 删行徽标（`f8839068`）、Provider 淡墨
  文字（`f4a14a19`）、「改动对新对话生效」文案（`c9f3a5cc`）
- 不写 notes：版本 bump（`e92a3201`）、IPC ts 镜像补齐（`33e5b753`）、v0.5.1
  状态同步（`3b546f61`）

## 门禁与一次撞红

- push main 后发现 `check.yml` 在 pill 提交（`12ba4cbb`）时就已经红了：**IPC
  protocol drift gate** 报 `set_reasoning_effort` 在 python / rust 里有、ts 里没
  有。这条命令只有 Core 发、GUI 不发，但 `gui/src/types/ipc.ts` 是全协议的 ts
  表面，Core-only 命令也要镜像（`generate_title` 就是先例）。补一个 interface
  进 union，本地跑完 check.yml 的六个门禁脚本再 push，第二轮三平台绿。
- 另一个隐藏事实：`check.yml` 的三个 Core 平台 job 只在「Frontend and managed
  payload」绿之后才起跑，所以前置门禁红时整轮 1m45s 就结束，看不到平台 job；
  绿的那轮 Windows Core job 跑了 15 分钟。
- 版本一致性 5 文件对齐；release run 35705638738 三平台绿（28–30 分钟），draft
  九资产齐全。
- Release notes：英前中后，六条 What's New，无 Under the Hood。覆盖审计逐条对
  8 提交，5 条用户可见各有 bullet，3 条有意省略。
- Smoke：JC 真机，批「smoke passed, publish and promote」。
- publish 09:12Z，GitHub Latest；promote run 35709073937 绿；本地
  `check-update-channel.mjs --cache-bust` 对 stable 验到 0.5.2 三平台，beta 别名
  同版本。

## 待观察

- app-update hop `v0.5.1 → v0.5.2`（SOP step 10）待 JC 装机验；`v0.5.0 → v0.5.1`
  从未报告、已核销。
- 迁移 042 在 JC 真实库上的结果：升级后设置 → 模型应显示老模型「跟随默认」、
  页尾默认配置里有上提的值、单独设过的模型「N 项覆盖」。
