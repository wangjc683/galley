# v0.4.13 发布

**日期**：2026-09-09
**上下文**：v0.4.12（2026-09-08）后一天内三轮设计讨论各落一块：CLI 与
Supervisor SOP 梳理、阅读面板扩展、侧栏时间桶。JC 真机验完三块后决定当天发。

## 范围（git log v0.4.12..，14 个提交）

按用户可见的变化收拢：

1. **阅读面板通吃可读文件**（`90cb7183`）——代码 / 数据 / 日志 / 图片引用
   进面板，CSV 表格、JSON 重排、图片标尺寸；脚本不给「用默认应用打开」；
   Git 审阅加比较基线；内置 GA 提示词要求用完整路径写文件。见
   [阅读面板扩展](./2026-09-09-reading-panel-files-and-git-baseline.md)。
2. **侧栏「本月」桶**（`cf8a9183`）——四桶滚动窗口、项目视图跟随 30 天、
   整窗为空回填 10 条。见 [侧栏时间桶](./2026-09-09-sidebar-month-bucket.md)。
3. **CLI 与 Supervisor SOP 梳理**（`c5e983de`）——`live` 字段（`status.running`
   恒 0 的解法）、broken pipe 静默、help 去代号；SOP / reference / skill /
   IM prompt 补 `--after-turn`、queued、`--mode=solo`、可逆分档；SKILL.md
   瘦身；PRD §11 命令表退役。见
   [CLI 与 SOP 梳理](./2026-09-09-cli-and-supervisor-sop-overhaul.md)。
4. **消息表格换行 + Git 行号栏**（`a427ffff`，09-08 截图重拍时顺手）。

其余 10 个提交是 README 定位 / 截图 v2、社区回帖记录、发布状态同步，
不进 notes。

Core 变化全是 additive（`sessions.run_state`、`local_file.access` 的
`text` / `image` kind、`git.review` 的 `base` / `log`），schemaVersion 不动。
`runner/` 与 `managed-ga/` 零提交。

## 定级

`v0.4.13`，patch。按 `v0.4.1` 规则只看最大单项：阅读面板从「仅 Markdown」
扩到可读文件是既有面上的增量，不是新产品线；侧栏加桶、CLI 加字段同理。

## 门禁

- **打包门禁不 mandatory**：`runner/`、`managed-ga/`、`bundle-python.sh`
  自 v0.4.12 起零改动；`release.yml` 平台 runner 在 tag 时照常守门。
- 本地：cargo workspace 全测（含 core 272 + cli 51/22/28）、gui typecheck /
  lint（0 warning）/ vitest 372、drift 与链接门禁，均在最后一个提交后跑过。
- `check.yml`：`90cb7183` 绿；`cf8a9183` tag 前等绿。
- 版本一致性：`check-version-consistency.mjs --tag=v0.4.13`。

## Smoke 计划（draft 门）

JC 装 draft 包一次覆盖：让模型存四种格式的文件到 `~/Downloads`，四个引用都
应带完整路径且可点，`.csv` 画表、`.json` 重排；Git 面板选一个老提交做基线；
侧栏出现「本月」段；`galley status` 带 `live`。Windows 真机过一遍。app-update
hop `v0.4.12 → v0.4.13` 发布后走 step 10。
