diff --git a/TMWebDriver.py b/TMWebDriver.py
--- a/TMWebDriver.py
+++ b/TMWebDriver.py
@@ -45,0 +46,2 @@ class TMWebDriver:
+        self.extension_client = None
+        self.extension_connected_at = None
@@ -93,0 +96 @@ class TMWebDriver:
+            if data.get('cmd') == 'get_status': return json.dumps({'r': self.get_status()}, ensure_ascii=False)
@@ -136,0 +140 @@ class TMWebDriver:
+                        driver._register_extension_client(self)
@@ -183,0 +188,4 @@ class TMWebDriver:
+    def _register_extension_client(self, client: WebSocket) -> None:
+        self.extension_client = client
+        self.extension_connected_at = time.time()
+
@@ -184,0 +193,2 @@ class TMWebDriver:
+        if self.extension_client == client:
+            self.extension_client = None
@@ -260,0 +271,11 @@ class TMWebDriver:
+    def get_status(self):
+        if self.is_remote:
+            try: return self._remote_cmd({"cmd": "get_status"}).get('r', {})
+            except Exception: return {'extension_connected': False, 'tab_count': len(self.get_all_sessions())}
+        sessions = self.get_all_sessions()
+        return {
+            'extension_connected': self.extension_client is not None,
+            'extension_connected_at': self.extension_connected_at,
+            'tab_count': len(sessions)
+        }
+
diff --git a/assets/tmwd_cdp_bridge/background.js b/assets/tmwd_cdp_bridge/background.js
--- a/assets/tmwd_cdp_bridge/background.js
+++ b/assets/tmwd_cdp_bridge/background.js
@@ -18,0 +19,7 @@ async function handleExtMessage(msg, sender) {
+  if (msg.cmd === 'wake') {
+    connectWS();
+    if (ws && ws.readyState === WebSocket.OPEN) {
+      setTimeout(() => { void sendTabsUpdate(); }, 0);
+    }
+    return { ok: true };
+  }
@@ -214,0 +222,5 @@ const WS_URL = 'ws://127.0.0.1:18765';
+const KEEPALIVE_MS = 25000;
+const PROBE_RETRY_MS = 3000;
+const PROBE_ALARM_MINUTES = 0.5; // Chrome MV3 alarms are 30s-level fallback.
+let keepaliveTimer = null;
+let probeTimer = null;
@@ -216,3 +228,4 @@ const WS_URL = 'ws://127.0.0.1:18765';
-function scheduleProbe() {
-  // Use chrome.alarms to survive MV3 service worker suspension
-  chrome.alarms.create('tmwd-ws-probe', { delayInMinutes: 0.083 }); // ~5s
+function clearProbeTimer() {
+  if (!probeTimer) return;
+  clearTimeout(probeTimer);
+  probeTimer = null;
@@ -221,3 +234,12 @@ function scheduleProbe() {
-function scheduleKeepalive() {
-  // Keep SW alive while WS is connected (~25s, under 30s SW timeout)
-  chrome.alarms.create('tmwd-ws-keepalive', { delayInMinutes: 0.4 }); // ~24s
+function scheduleProbe() {
+  // A Galley probe starts a short-lived local WebSocket server. If the
+  // extension only waits for a coarse MV3 alarm, repeated manual tests can
+  // miss that server on Windows. Retry quickly while the service worker is
+  // alive, and keep an alarm fallback in case Chrome suspends it.
+  clearProbeTimer();
+  probeTimer = setTimeout(() => {
+    probeTimer = null;
+    if (ws && ws.readyState <= 1) return;
+    connectWS();
+  }, PROBE_RETRY_MS);
+  chrome.alarms.create('tmwd-ws-probe', { delayInMinutes: PROBE_ALARM_MINUTES });
@@ -225,10 +247,17 @@ function scheduleKeepalive() {
-
-async function isServerAlive() {
-  try {
-    const ctrl = new AbortController();
-    setTimeout(() => ctrl.abort(), 2000);
-    await fetch('http://127.0.0.1:18765', { signal: ctrl.signal });
-    return true; // Got HTTP response → port is listening
-  } catch (e) {
-    return false; // Network error (connection refused) or timeout → server not alive
-  }
+function clearKeepaliveTimer() {
+  if (!keepaliveTimer) return;
+  clearTimeout(keepaliveTimer);
+  keepaliveTimer = null;
+}
+function scheduleKeepalive() {
+  clearKeepaliveTimer();
+  keepaliveTimer = setTimeout(() => {
+    keepaliveTimer = null;
+    if (ws && ws.readyState === WebSocket.OPEN) {
+      try { ws.send('{"type":"ping"}'); } catch (_) {}
+      scheduleKeepalive();
+    } else {
+      ws = null;
+      scheduleProbe();
+    }
+  }, KEEPALIVE_MS);
@@ -243 +271,0 @@ chrome.alarms.onAlarm.addListener(async (alarm) => {
-    // Keepalive: ping to keep SW alive + detect dead connections
@@ -253,0 +282 @@ chrome.alarms.onAlarm.addListener(async (alarm) => {
+    clearProbeTimer();
@@ -255,6 +284 @@ chrome.alarms.onAlarm.addListener(async (alarm) => {
-    if (await isServerAlive()) {
-      console.log('[TMWD-WS] Server detected, connecting...');
-      connectWS();
-    } else {
-      scheduleProbe(); // Server not up, keep probing
-    }
+    connectWS()
@@ -337,0 +362 @@ function connectWS() {
+  clearProbeTimer();
@@ -344,0 +370 @@ function connectWS() {
+    clearKeepaliveTimer();
@@ -349,0 +376 @@ function connectWS() {
+    clearProbeTimer();
@@ -388,0 +416 @@ function connectWS() {
+    clearKeepaliveTimer();
diff --git a/assets/tmwd_cdp_bridge/content.js b/assets/tmwd_cdp_bridge/content.js
--- a/assets/tmwd_cdp_bridge/content.js
+++ b/assets/tmwd_cdp_bridge/content.js
@@ -2,0 +3,10 @@
+if (window.self === window.top && /^https?:/.test(location.href)) {
+  try {
+    chrome.runtime.sendMessage({
+      cmd: 'wake',
+      url: location.href,
+      title: document.title
+    }).catch(() => {});
+  } catch (_) {}
+}
+
diff --git a/ga.py b/ga.py
--- a/ga.py
+++ b/ga.py
@@ -113,0 +114,11 @@ driver = None
+NO_BROWSER_TABS_MSG = "浏览器插件已连接，但没有可用网页。请在同一个 Chrome / Edge 里打开一个网页，然后重试。"
+NO_BROWSER_CONNECTION_MSG = "浏览器当前没有连接。若已安装插件，请打开同一个 Chrome / Edge 的网页后重试；首次使用请先在 Galley 顶部的浏览器控制入口完成设置。"
+
+def browser_control_empty_msg():
+    try:
+        if driver is not None and driver.get_status().get('extension_connected'):
+            return NO_BROWSER_TABS_MSG
+    except Exception:
+        pass
+    return NO_BROWSER_CONNECTION_MSG
+
@@ -118,2 +129,4 @@ def first_init_driver():
-    for i in range(7):
-        time.sleep(2)
+    deadline = time.time() + 12
+    sess = []
+    while time.time() < deadline:
+        time.sleep(1)
@@ -122 +135,4 @@ def first_init_driver():
-        if i == 4: webbrowser.open("https://example.com")
+        try:
+            if driver.get_status().get('extension_connected'): break
+        except Exception:
+            pass
@@ -133 +149 @@ def web_scan(tabs_only=False, switch_tab_id=None, text_only=False, maxlen=35000)
-            return {"status": "error", "msg": "没有可用的浏览器标签页，查L3记忆分析原因。"}
+            return {"status": "error", "msg": browser_control_empty_msg()}
@@ -179 +195 @@ def web_execute_js(script, switch_tab_id=None, no_monitor=False):
-        if len(driver.get_all_sessions()) == 0: return {"status": "error", "msg": "没有可用的浏览器标签页，查L3记忆分析原因。"}
+        if len(driver.get_all_sessions()) == 0: return {"status": "error", "msg": browser_control_empty_msg()}
