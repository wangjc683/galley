# 03 残余原生手段实验

Status: needs-info

仅当 issue 01 判定「裸 app 也死」(H1 出局)后才进行——若凶手是
Galley 窗口栈,修栈即可,不该再堆原生补丁。前一轮已证死路
(死循环/拉锯战)的手段见 PRD「历史」,不得重试。

## 未试手段(devlog 排序)

1. `AttachThreadInput` + `SetFocus` 组合(跨线程焦点经典偏方;
   注意仍须遵守「焦点已在 webview 子树内不得干预」铁律)。
2. 模拟真实点击:`PostMessage WM_LBUTTONDOWN/UP` 到
   `Chrome_WidgetWin_1`(唯一确认有效的动作是真实鼠标点击,
   此为最接近的程序化模拟;注意别真触发页面点击副作用——
   可打到无控件坐标)。

## 约束

- 任何断言必须带终止条件(generation 去重 / 幂等守卫),严禁
  重现 v0.3.7 死循环。
- 若两条全败:接受现状为已知限制,等上游(issue 02),不做
  产品层提示控件(需要解释的控件 = 设计失败)。
